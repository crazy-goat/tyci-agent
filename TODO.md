# tyci — backlog

Working agreement: one item at a time. A Sonnet subagent implements, an Opus
subagent reviews, the Sonnet subagent fixes what the review found, and that
loop repeats until the review comes back clean. Then the next item.

The loop runs without asking which item comes next: pick one, implement, review, fix, merge, push, report. Only stop to ask when a decision is genuinely the owner's — a semantic choice, a trade-off with no obviously right answer, or something that changes what gets built. Ordering is not one of those.

Status: `todo` · `wip` · `review` · `done`

Work happens in a git worktree, never in the main checkout. A coder subagent
gets `isolation: "worktree"`, the review runs against that worktree, and only a
feature that came back clean is merged into `main`. The reason is not tidiness:
the main checkout is where the person reads code and thinks about what to build
next, and a tree carrying three half-finished features is unreadable for that.
`main` therefore stays committed and clean between items. **Every closed step ends with a push**: merge the clean branch into `main`, run the full suite there, `git push origin main`, then remove the worktree and delete its branch. Nothing finished is allowed to exist only on this machine — that is how a session's work was nearly lost once already, sitting on an unpushed worktree branch that a cleanup would have deleted.

The loop feeds itself. Anyone working an item — coder or reviewer — who trips
over a real problem outside their scope does NOT fix it and does NOT stay quiet
about it: it goes in the Inbox below, one line, with a file:line and why it
matters. Fixing it inline would smuggle unreviewed work into an unrelated diff;
dropping it means finding it again in three weeks. Between rounds the inbox is
triaged — promoted into the tables with a size, merged into an existing item, or
struck out with a reason. The queue is expected to grow faster than it drains at
first; that is the point, not a failure.

## Where things stand

Everything reviewed and clean is on `main` and pushed. (Historic note,
2026-08-23: the two parked branches below are gone — items 17 and 8 both
finished their review loops and were merged to `main`; this section had said
they were blocked long after that stopped being true.)

Operational notes that are easy to lose:

- `~/.tyci/providers.json` now carries a hand-fetched `nexos` provider (56
  models, real gateway prices, from `GET https://api.nexos.ai/v1/models`,
  which returns per-token costs that must be multiplied by 1e6). models.dev
  does not carry it, so a refresh must never overwrite it — that is what item
  18's merge behaviour protects, and there is a test for it. Backups from that
  fetch sit alongside with a `.bak-<timestamp>` suffix.
- These prices include whatever markup the gateway applies, so they are the
  only correct source for this setup; the public models.dev prices for the
  same models would understate the bill.
- The cost figure in the status bar is recorded per model call through a
  wrapped Sink, not once per turn, which is why it appears with the first
  response.

## Bugs and dead code (cheap, unblock what already exists)

| # | Item | Size | Status |
|---|------|------|--------|
| 12 | `tyci session show` parses the wrong event type and field names (`sessioncmd.go:165-183`): looks for `type:"header"` where the file writes `"session"`, and a `blocks` field where the format has `content` with `toolCall`/`toolResult`. Result: empty header, 0 tool calls. Also fixed on the way: an infinite loop on a corrupt or truncated line (`json.Decoder` never resynchronises), a silently-dropped remainder when a line exceeds the scan buffer, and a test fixture that invented a block type no writer produces. | XS | done |
| 13 | Two loose ends: `stream.Retry` is emitted but never consumed in `runOnce`'s switch; and the subagent tool whitelist check (`main.go:355-366`) omits `alwaysAllowedTools`, so `help`/`lua` appear in a whitelisted child's schema but a direct `help` call is refused. Resolved: `stream.Retry` handling removed rather than half-implemented (see F1), and the gate/schema disagreement fixed from one shared list — including the unrestricted path, where `agents` was reachable through `lua` despite never being offered. | XS | done |
| 18 | **models.dev prices survive a refresh, and a refresh stops destroying what it did not fetch.** Mostly landed: `Cost`/`Limit` are carried through the cache, `RefreshModels` merges instead of overwriting (a hand-maintained gateway provider survives), the write is atomic (temp + rename, mirroring `internal/cron/store.go`), and an import of nothing no longer rewrites the file. **Left over from review:** (a) whole-provider replacement is unconditional, so a degraded fetch serving a provider with zero models silently empties a good priced entry — skip the swap when the incoming provider has no models and the cached one does; (b) `CatalogNeedsPrices` is global where the problem is per-provider, so one priced provider (the user's `nexos`) suppresses the warning for the other 127 unpriced ones forever, and it duplicates `pricing.CatalogPriced` in a second package — better to warn about the model actually in use, which is the only moment the message is true and actionable; (c) untested paths: `--provider x` against an existing catalog, and the unparsable-existing-file refusal that is this item's actual data-loss guard. | S | done |
| 20 | **Every quick blocking subagent runs TWICE.** Present on `main` today, not introduced by our work. `runWithHandoff` (`tools/subagent.go:696`) returns `(ToolResult{}, false)` at line 744 when every child finished inside the 60s window — the common case — which discards the results it already has; the caller at `tools/subagent.go:353` then falls through and re-runs every task from scratch (`:364`). So one `subagent(task=…)` call pays the model twice, performs its side effects twice (file writes included), streams into the parent's tool block twice, and writes two `resumable` transcripts. Verified empirically by the reviewer: runner invoked 2×, and after item 17 it also registers 2 jobs. Fix belongs in `Run`/`runWithHandoff` — return the collected `st.result()`s when `handed == false` instead of falling through — not in the fallback function. Merged (`6e88ace`). Was to be done BEFORE item 17 is merged: it doubles item 17's blast radius, and items 15/16 build on the same call path. | S | done |
| 8 | MCP is not wired on live paths: `tools.InitMCP` (`tools/mcp.go:257`) has no caller outside tests, and the agent config uses `GetToolsSchemaJSON()` rather than `GetAllToolsSchemaJSON` (`commands.go:156`). MCP servers are never connected nor advertised to the model. Fixed on `worktree-agent-a0ff0141823ce5ee4` (`65db162`), in review. Ground truth checked first: the `mcp:` counter was an honest 0 rather than a lie, and there is no `~/.tyci/mcp.json` on this machine, so the feature stays dormant until a server is configured. **DONE — both batches merged.** Batch 2 wired it up: `initCommon` connects and returns a shutdown func, `run`/`console`/`tui` all connect (so cron, which shells out to `tyci run`, gets the same tool set as the TUI), `--no-mcp` opts out, and the schema comes from `GetAllToolsSchemaJSON`. Whitelist policy as decided: an agent declaring `tools:` gets an MCP tool only by naming it or via an `mcp_<server>_*` wildcard resolved against the tool's **actual** owning server — bare `mcp_*` is not legal. Two extra gaps found on the way: `GetSubagentToolsSchema` never appended MCP tools at all, so a child would not have seen them even once connected; and making connects concurrent introduced a `concurrent map writes` crash on the shared unsynchronised `AuthManager` (tokens are now resolved serially before any goroutine starts) — a revert of that fix fails 30/30 under `-race`. Measured: **37 µs and zero goroutines with no `mcp.json`**, so no cron regression, and exactly 5 s total against a dead server. **Batch 1: the stdio client.** The P0 deadlock, F5, both F8 nil-derefs, the map-order schema and the `npx` orphaning are all fixed on `main` with tests that were each verified to fail when their fix is reverted; `MCPToolRunner.Close` also closes servers concurrently instead of serially under the lock. **What remains for batch 2 is the wiring itself** (still on `wip/8-mcp-wiring`, cut before `main` moved) plus the four review findings below. One new constraint from batch 1's review: `MCPToolRunner.Close` is no longer serialized by `r.mu`, so once batch 2 adds shutdown paths, two concurrent calls would run `StdioClient.Close` twice on the same client and race on `exec.Cmd.Wait`'s `ProcessState` — add a `sync.Once` or a `closed` flag. Also do not start calling `sendNotification` from a new goroutine: it is the one remaining unlocked `c.stdin` read, safe only because `Initialize` is its sole same-goroutine caller. Original P0 note, kept for the record: **every stdio MCP server self-deadlocked.** `internal/mcp/stdio.go:50-51` takes `c.mu` and holds it for all of `Initialize`, which then calls `sendRequest` — which locks `c.mu` again at `:212`. `sync.Mutex` is not reentrant, so it deadlocks immediately; `Close` at `:83/88/95` would too. Proven by running it in isolation, not inferred. Pre-existing, and invisible to the tests because the only stdio case fails at `cmd.Start()` before reaching it, and every successful-connect test uses HTTP, whose `sendRequest` takes no lock. Consequence of wiring MCP up without fixing it: the ordinary `{"command":"npx"}` config gives a 5-second startup stall, a scary warning, a leaked goroutine and child process, and zero MCP tools — i.e. exactly the "MCP is broken" conclusion this item set out to remove. Fix before merging, with a real stdio test (a `sh -c` script echoing a valid JSON-RPC initialize result). Then four more from the same review: **(a)** the `tools:` whitelist exemption is skew-free (gate and schema agree — that part is right) but wrong as a default, since it hands every agent every tool on every server, including write-capable ones, and `mcp_<server>_<tool>` names *are* authorable once `mcp.json` exists — honour explicit entries, or a `mcp_weather_*` wildcard the definition opts into; **(b)** `os.Exit` paths skip the deferred shutdown, `Close` is an immediate `SIGKILL` that never waits (F5) and never reaps, and killing an `npx` wrapper orphans the real `node` — needs `Setpgid` and a signal to the group; **(c)** `run` (and therefore every cron job, `internal/cron/run.go:14`) silently has a different tool set from the TUI, which is the hardest failure mode to debug, and the stated cost is overstated since connects are concurrent and capped at 5s total — connect there too with an opt-out; **(d)** no test fails if the wiring itself is reverted — `initCommon` has no test coverage at all, which is the very class of bug this item fixes. | S | done |
| 24 | **The todo list is one global variable for the whole process, so every agent shares it.** `todoState` (`tools/todo.go:31`) is a package-level var and the registry holds a single stateless `&TodoTool{}` (`tools/tool.go:596`), so main, `/btw` and every subagent read and write the same list. Reported from use: todos appearing to survive `/new`, and todos crossing between subagents. The second is certain from the code. The first is subtler — **both `/new` paths do call `tools.ClearTodoList()`** (`tui_mode.go:255`, `interactive.go:166`), the top bar reads counts live (`display/tui_status.go:142`), and the `/new` refusal in `display/tui_keys.go:134` applies only mid-turn (`if m.reading` returns early when idle) — so the clear is not missing. What defeats it is the shared global: any child still running after `/new` immediately writes its todos back into the list the user just cleared. Likely one bug with one fix. Consequences beyond mixed-up entries: the plan guard leaks **child to parent**, not parent to child — corrected after checking the code: `HasTodos`/`PendingTodos` are wired only on the main path (`commands.go:171-173`) and `enforcePlanGuard` returns early when `cfg.HasTodos == nil` (`agent/tools_exec.go:32`), and the child config at `main.go:285` never sets them, so children never had the guard to inherit. What did happen is the reverse: the parent's guard read the global list, so **a child adding todos satisfied the parent's guard** and let the parent skip planning; `todo(action="clear")` in a child wipes the parent's plan; ids collide, so the parent's `update id=7` can hit a child's item; and the end-of-turn reminder (`agent/agent.go:291`) shows the parent someone else's work. Fix: per-agent todo state, keyed the way the other per-agent tool state already is (a ctx key set at spawn, cf. `SubagentSinkCtxKey`/`JobIDCtxKey`), with the six package-level readers (`AllTodoItems`, `TodoCounts`, `PendingTodos`, `HasPendingTodos`, `ClearTodoList`, and `Run` itself) taking the agent's list rather than the global one. The TUI must keep showing the **main** agent's counts, not whichever child wrote last. Note item 1's Subagents tab would want a child's own list, so do not throw the per-child lists away on completion. | S–M | done |
| 9 | User-defined Lua tools in `~/.tyci/tools/` and `./.tyci/tools/` are loadable and callable through `RunTool`, but their schemas never reach the model — `GetToolsSchema` (`tools/tool.go:117`) is a static list. Also: the parsed `parameters`/`runFunc` are stored and unused, and `Run` re-DoFiles the script on every call. **Merged.** `GetToolsSchema` now appends a schema entry per registered `*LuaTool`, flowing unchanged through `GetAllToolsSchema`/`GetTopLevelToolsSchema`/`GetSubagentToolsSchema` (no gating regression — Lua tools are meant to be visible everywhere). `Run` no longer `DoFile`s from disk every call: a `sync.Once`-guarded `*lua.FunctionProto` is compiled once and shared read-only across fresh per-call `*lua.LState`s (verified against gopher-lua's own top-level-chunk construction; `-race` clean under concurrent calls). Reviewed independently; one real finding fixed before merge — `luaToolsSchema()` iterated a map with no sort, reordering the tools block every turn and risking cache-busting on the provider side, same class of bug item 8 already fixed for MCP tools. Fixed to sort by name. | S | done |
| 10 | No history compaction and no context-budget accounting. The session format already has a `compaction` event and `session.WriteCompaction`, and replay applies it — nothing ever writes one. Needs `/compact` (with optional focus instructions) and an auto-compact threshold. **Also a model-facing `compact` tool plus a budget reminder** — the model compacts itself instead of waiting for the user, since quality visibly degrades past ~100k tokens while cost keeps climbing. Design points settled in a side-conversation: **(a) compaction is a view over the log, never a deletion** — the raw JSONL stays and the event only says "read the summary from here", so a bad choice by the model loses nothing; `sanitizeMessageSequence` (`session/session.go:565`) is already called by the compaction replay path, so a cut landing inside a tool-call/result pair is handled. **(b) The reminder must state a fact, not a diagnosis** — "you are at 120k of 200k" rather than "your thinking is degrading". The model at 100k is the worst available judge of its own degradation, and a panic instruction produces a panicked summary; a number it can act on does not. **(c) Reminders fire only at turn boundaries** — `agent/agent.go:290-322` injects them when the model believes it is finished (`!more && !drained`), with a per-turn cap. That is the right moment for compaction (no half-finished tool loop) but it will not interrupt one long turn; wanting mid-turn reaction is different machinery. **(d) Let the model persist what matters before compacting** — to `memory/` or a file — so the summary is not the only surviving artefact. **(e) Every compaction writes a full markdown dump of the session and hands the model its path** — "if you need it, the whole record of what you did is here". So the flow is: the model persists what matters (memory or a file), compaction writes the dump, and the model comes back holding a path rather than only a summary. **The dumps exist to be searched, not read** — `grep`/`rg` over the session directory is the primary interface, which constrains the format more than prettiness does: **never soft-wrap** (a wrapped line splits a path or identifier in half and grep stops finding it); make **every line self-identifying** with a short stable prefix (e.g. `[12] [bash]`) so a hit tells you where it came from without jumping around the file; write **paths, tool names and error text verbatim** rather than paraphrased, since those are exactly the search terms; **strip ANSI escapes**, or `grep error` misses lines whose text is interleaved with colour codes; and include tool inputs and outputs (under a size cap) because "which file did I edit" and "what was that error" are the questions actually asked. Since there are many files, `rg` across the directory is the normal usage, so filenames should be meaningful rather than bare hashes. **One markdown file per session, not one nested document** — decided, and it removes most of the machinery: no recursion, no `seen` map guarding cycles, no heading-level arithmetic. The main session's file carries a line per child linking to that child's own file; links are relative and every file lives in the same session directory, so the directory can be copied or zipped and the links still resolve. A child's file is written when the child finishes rather than re-derived at compaction time, and a long session no longer produces one enormous document — you open only the child you care about. This dovetails with item 16: the path to a child's markdown is a natural part of the "here is how to come back to me" line. Prior art the owner already built: `~/work/tokeneks`, `web_detail.go` — `handleAPISessionMarkdown` and `appendMarkdownSession` are worth reading for the per-step shape (timestamp, model, user prompt, thinking, tools) and the metadata header (agent, session id, project, model, date); its recursive nesting and cycle guard are what the file-per-session split makes unnecessary. tyci reaches children through the `resumable` transcript map (`main.go:59-79`) and the jobs registry. Two decisions taken up front: the dump lives **beside the session JSONL** (`session.SessionDir`) as a sibling file, so it is found with the session and discarded with it rather than starting a second store; and the dump is a **derived artefact, never a second source of truth** — it is regenerated from the JSONL, so compaction can never be an unrecoverable operation. Reuse the decoding path `session show` already uses (fixed in item 12) instead of writing a third parser. The same renderer gives `tyci session export --markdown` for free, which is useful on its own. Related, worth its own thought: some context bloat is file-searching that never needed to enter the main thread at all. A subagent is often cheaper than a compaction. **Merged.** `/compact` (optional focus instructions), a model-facing `compact` tool, and a factual context-budget reminder (real measured usage including cached tokens, injected only at turn boundaries with a per-turn cap) are all shipped, backed by `session.WriteCompaction`. Every compaction writes a grep-friendly per-session markdown dump (non-wrapping, self-identifying line prefixes, ANSI-stripped, size-capped, ANSI/ansi/verbatim paths and tool names) beside the session JSONL, reusing the existing session-file decoding path; the dump path is folded into the conversation itself, not just printed to the terminal. Two review rounds (Opus, mutation-verified by reverting each fix). **Left for the Inbox, not done here:** no auto-compact threshold (only the reminder), no `tyci session export --markdown` subcommand, no child markdown files/parent links (subagent children have no `Session` today), `LoadForReplay` still skips `SanitizeMessageSequence` on the `/resume` path, and `refreshMarkdownDump` still rewrites the whole dump on every message write — see F5-F9. | M | done |
| 11 | Cron only fires while an interactive session is open, and the `tyci cron` command that `internal/cron/run.go:77` refers to does not exist. | M | todo |
| 32 | Project-local discovery uses raw cwd instead of the repository root (`internal/agentdefs/agentdefs.go:94-99`, `agent/agents.go:124-125`). Use the same `gitinfo.ProjectRoot`/`session.ProjectKey` rule as sessions and project config, so running from a subdirectory or linked worktree finds the local agents and legacy config. | S | done |
| 33 | Sidebar orphan chains can be emitted child-before-parent when orphan keys sort unfavourably (`display/tui_sidebar.go:850-871`). Build the orphan tree parent-first and add a regression test. | XS | done |
| 34 | Sidebar job selection has stale/incorrect row mapping: add coverage for a job list shrinking without resize while cursor/scroll is stale (`display/tui_sidebar.go:197-221`), and for Tasks headers/root plus nonzero scroll (`display/tui_sidebar.go:511-521`). Verify rendered-line hit-testing maps to the correct job and clamps scroll before selecting it. | XS | todo |
| 35 | Avoid rebuilding the complete sidebar tab content twice per frame (`display/tui_sidebar_view.go:208-229`, `display/tui_sidebar.go:193-221`); pass the computed line count through the scroll clamp. | XS | todo |
| 36 | The non-TUI console has no visible jobs/waiting-question view. Add a compact jobs/status path so a person can notice a child waiting for input without relying on model relay (`display/tui_jobs_panel.go:59-106`). | S | todo |
| 37 | Bare `/btw` and `/resume` at an idle TUI prompt can leave the model in an active-reading state (`display/tui_mode.go`). Ensure every successful command path restores the idle/read state and add regression tests. | XS | todo |
| 38 | Recover panics from job functions in `jobs.Registry`, mark the job failed, and keep the interactive process alive (`jobs/registry.go:66`). Add a test proving a panicking child does not crash the registry/process. | S | done |
| 39 | Bound and prune the `resumable` transcript map (`main.go:73-80`, `btw.go:173-180`) so finished children do not retain full message histories forever; preserve ids still needed by active jobs. | S | todo |
| 40 | Keep consuming `SIGINT` while signal handling is installed (`prompt_mode.go:57-69`). Repeated Ctrl-C must cancel/interrupt the current operation instead of being silently swallowed. | XS | done |
| 41 | **Child prompt no longer contradicts its own `ask_parent` tool.** `providers/provider.go:29` said "You cannot ask questions — there is no user to reply" three lines above the same prompt handing the child `ask_parent`, and the shared header at `:117` repeated it ("There is nobody to ask"). Children had to disbelieve their own contract to use the tool they were given. Fix: the child contract now leads with decide-and-act, then names `ask_parent` as a real tool gated three ways (hard blocked / ANY assumption would make the work useless or unsafe / never for preference or style) and priced (full stall, fails if nobody can reach it); the header is gated like `posture`/`contracts`/`toolLines` already were, so the parent keeps "nobody to ask" (true — it has no `ask_parent`) and the child gets "no interactive user". Guarded by `TestSubagentPromptDoesNotContradictItsOwnAskParentTool` plus a test for the guard itself: the first version was a single regexp that missed BOTH deleted strings while false-tripping on "never ask for permission", so a regexp matching nothing would have passed as quietly as a working one. Reviewer checked the opposite failure too (over-correction into advertising the tool) and cleared it. Out of scope, filed: a named agent whose `tools:` whitelist omits `ask_parent` still gets told it is real. **Merged.** | XS | done |
| 42 | Make child todo content lookup agent-specific rather than keyed only by todo id (`display/tui_render_tool.go:252-261`), so a child's id 1 cannot be enriched with main agent's item 1 when child transcripts are rendered. | S | todo |
| 43 | Preserve or explicitly reset the todo plan when `resume` re-keys a conversation (`btw.go:110-113`); a resumed agent must not see transcript references to todo items that silently disappeared. | XS | todo |
| 44 | Show named agents' tool allowlists in the list output (`tools/agents.go:63-71`) and keep the completion/UI entry consistent, so the model does not select an agent that cannot perform the requested work. | XS | todo |
| 45 | Make MCP stdio response delivery non-blocking or cancellation-aware (`internal/mcp/stdio.go:327-331`); a duplicate response after its waiter exits must not hold `c.mu` forever and wedge `Close`. | S | todo |
| 46 | Decide and document whether Windows is a supported target. If yes, fix the unix-only build failures (`internal/readline/read.go:62`, `tools/bash.go:411,427`); if no, add an explicit support/build note rather than leaving the target accidentally broken. | S | todo |
| 47 | Make the missing-price warning actionable for providers absent from models.dev (`commands.go:229-230`, `internal/pricing/pricing.go:150-156`); do not prescribe a refresh that cannot fetch a hand-maintained/local provider. | XS | todo |
| 48 | Reuse `subagentModalLayout().contentHeight` in all modal scroll/page calculations (`display/tui_modal_view.go:10-18`) instead of duplicating popup-height arithmetic, and add a small-terminal regression test. | XS | done |
| 49 | **Read-only viewer rozmowy subagenta w głównym oknie.** Enter/click w Tasks powinien otwierać pełny transcript joba w viewerze, bez podmieniania głównej sesji; zamknięcie wraca do głównej rozmowy. Wykorzystać kopię z `resumable` pod mutexem, osobny viewer i zachować `r` jako kontynuację przez `resume`. Zakres obejmuje też poprawne mapowanie wiersza Tasks przy nagłówkach/root/scroll. | M | todo |
| 52 | **Prompt input nie rósł przy soft-wrapie — tekst znikał za prawą krawędzią.** `capInputHeight()` (`display/tui_input.go`) liczyła wysokość z `textarea.LineCount()`, a to liczba linii LOGICZNYCH (po twardych enterach); soft-wrap jej nie zwiększa, więc długi jednolinijkowy prompt zostawał przy wysokości 1 i viewport textarea scrollował w dół zamiast łamać wiersze. Fix: nowy helper `inputWrappedRows()` (`display/tui_input_wrap.go`) sumuje zawinięcia per linia logiczna używając verbatim kopii prywatnego `wrap` z bubbles v1.0.0 (unicode.IsSpace + uniseg.StringWidth + go-runewidth, łącznie z trailing-space zachowaniem na końcu tekstu) przy `m.input.Width()` (= WEWNĘTRZNA szerokość zawijania widgetu: SetWidth odejmuje ramkę i prompt; świeży textarea ma Width()=34, produkcyjne SetWidth flooreszą do ≥10). Podmienione też 4 bloki pre-set wysokości przed insertem newline w `tui_keys.go` (idle Alt+Enter/Ctrl+N/Ctrl+J + busy path), ten sam clamp [1,10]. Quirk pinowany testami: dokładnie-width znaków łamie się na 2 wiersze (trailing-space bubbles), nie na 1. Oracle testów: `LineInfo().Height == len(memoizedWrap(...))`. Pre-fix potwierdzony (4 testy failują na starym kodzie), review niezależne, `-race` czysty. **Merged.** | S | done |

## Features

| # | Item | Size | Status |
|---|------|------|--------|
| 54 | **Late-bound notices, so a question is deduplicated by delivery rather than by guesswork.** Batch 2 fixed the lost-question bug but had to accept a duplicate in exchange: a child that asks during a blocking `subagent` call now surfaces its question twice — immediately in the handoff message (`spawnedJobsMessage`'s `question` field) and again as a queued notice. The reason dedup cannot be done at ask time is an ordering fact: `Registry.Ask` calls `onEvent` **synchronously before it blocks**, so the notice is queued strictly before `pendingQuestions` can run. Suppressing at ask time therefore means *predicting* that the handoff text will carry the question, and that prediction is wrong in at least three reachable cases — `handOff` returns early when `len(stillRunning) == 0` and never peeks, `handoff == false` takes `cancelRemaining` instead, and an observer can be watching when no handoff ever happens. Keying suppression on "an observer exists" is lossy again, the same class as the bug batch 2 fixed. *Withdrawal* of a queued notice is also wrong: `runWithHandoff` calls `handOff` from its `<-ctx.Done()` branch too (Esc), where the tool result plausibly never reaches the model, and today's notice is exactly what covers that. **The sound design:** hold notices as keyed entries (`job id` + question) rendered at **drain** time rather than as flat strings queued at event time, and let the handoff-carried question set an "already shown" flag checked then. That is non-lossy because `agent/agent.go` drains `NextMessages` only *after* tool results are appended — so at drain time it is a fact, not a prediction, that the handoff text reached the model. Scope is a refactor of the notice queue (`jobs/notify.go`, the `NextMessages` drain, and `main.go`'s onEvent hook), which is why batch 2 deliberately did not attempt it. Until it lands the duplicate is matchable by the model, since notice and handoff JSON both carry `job_id`, and `spawnedJobsMessage`'s prose now says a later notice for the same id is the same question restated. | M | todo |
| 53 | **`report_progress` history, bounded.** `Registry.SetProgress` overwrote a single `Job.Progress`, so three reports left only the third. Fix: `Job.ProgressHistory` (cap 20, oldest evicted) + `ProgressHistoryTruncated`, entries rune-capped at 2000, deep-copied in `Snapshot()` like `ResidualMailbox`, and independently re-bounded on the tombstone path rather than trusting the live cap. `wait` renders a bulleted, newline-separated block under a single AGGREGATE 800-rune budget (same idiom as `subagentNoticePreviewLimit`), newest kept, with one honest parenthetical folding the exact budget-drop count together with the countless registry eviction. **The premise both rounds started from was wrong and review caught it:** the dominant producer is not `report_progress` at all but `tools/bash.go:489`, which posts EVERY output line of a backgrounded command at ~1/s — so the cap is a sliding tail over a steady stream, not an overflow guard for a rare event, and the first `" -> "`-joined rendering turned a backgrounded `go test` into one glued line. Entries are flattened and blank ones skipped for the same reason. **Decision: the panel keeps showing the latest note only** — same reasoning as `PendingLines`, it is a compact one-line-per-job view (its own comment says width is tight); the history's consumer is the parent agent through `wait`. That supersedes this item's original "give the panel somewhere to show it". Also fixed the comment in `tools/progress.go` claiming the panel never shows `Progress` — `display/tui_jobs_panel.go:208` does. **Merged.** | S | done |
| 51 | **Styled markdown while the text is still streaming.** Today markdown is glamour-rendered exactly once, when a block finishes (`forceRenderDirtyBlocks`, `display/tui_markdown.go:111`); during the stream the user reads raw, unstyled text re-wrapped one line at a time by `streamWrap` (`display/tui_render_block.go:9-63`), with transport-level coalescing at 33/100 ms (`display/tui_api.go:183`). That is cheap and correct, but a long answer is read *as it arrives*, and reading unstyled markdown for thirty seconds is the actual pain. Decided (owner, full variant): render the safe prefix progressively at blank-line boundaries and keep the tail raw. **The safe-boundary rule is one sentence: a `\n\n` is a safe flush point iff no fenced code block is open there.** Tables need no state at all — a GFM row is a single line and a table ends at the first non-row line, which a blank line always is, so by the time we see `\n\n` the table is already over; a lone `|` line with no delimiter row was never a table. Fences are the only construct that genuinely spans blank lines with no single-line terminator, so track them: marker char (`` ` `` vs `~`), opening run length (a fence closes only on a run **≥** its opening, so a 3-backtick line inside a 5-backtick fence must not close it), and indent 0-3. Indented 4-space code blocks are symmetric to fences and deliberately **out of scope** per the owner's narrowing — worst case one block flushes as two for a few hundred ms. Setext headings cannot straddle a boundary (CommonMark forbids a blank line between text and underline), and front matter does not occur mid-reply. **Why mid-stream imperfection is acceptable: the whole-content render at block finish already exists, so every artifact is self-healing** — this is what removes the correctness objection to the whole idea. Scan is incremental (`O(new bytes)`, anchored on the last `\n` exactly as `streamWrap` already does at `display/tui_render_block.go:37`), never re-scanning history; each byte reaches glamour at most once because segments partition the content, so this must not reintroduce the O(n²)-per-token pathology `appendOrAppend`'s comment warns about (`display/tui_blocks.go:276-320`, issue #84). New per-block state (`inFence`/`fenceMarker`/`fenceLen`/`fenceIndent`/`safeUpto`/`renderedUpto`/`renderedPrefixLines`) keyed by block index next to `streamWraps`, with the **same lifecycle at every site that clears `streamWraps`** (`tui_blocks.go:192`, `tui_scroll.go:92`, `tui_scrollback.go:273`, and the deletes in `tui_markdown.go:142,164,177,180`) — a missed clear site is the likely first bug. `streamWrap` must be rebased to wrap `content[renderedUpto:]` rather than byte 0. Reuse `getRenderer`'s width-keyed cache and `collapseMarkdownBlankLines` per segment, both unchanged; `mdCacheRendered` keeps meaning "final render" only. Text blocks only — thinking blocks render a single frozen summary line (item 19), so the feature has no visible effect there. **The one risk no unit test can settle:** glamour wraps differently from raw text (it strips `**`, heading markers, link syntax), so a segment changes line count when it flips from raw to styled, and the viewport is pinned to bottom while streaming — so expect a visible hitch on *every* flush, roughly 10-30 per message. Owner chose the full variant over the narrower "flush only when a fence closes" (0-3 hitches) deliberately; if the hitch proves unbearable in live use, that narrower version is the fallback and needs the identical state machine. Accepted artifacts, all self-healing: nested-list indentation lost across a flush; ordered-list numbering across a flush (**verify, do not assume** — goldmark should start at the literal number on the segment's first item); split indented code blocks. Test plan: fence with interior blank lines fed at arbitrary chunk splits including mid-marker; 5-backtick fence containing a 3-backtick line; table with and without a delimiter row; **a message with no blank line at all, asserting output stays byte-identical to today's pure `streamWrap` path** (the no-dead-air regression guard); list numbering across a flush recording ground truth; CJK/emoji width at a boundary; chunk-boundary independence extending `TestStreamWrapMatchesFullWrap` (`display/tui_perf_test.go`); a partition guard proving no byte is rendered twice; and the property that operationalizes self-healing — **the final render must be byte-identical to rendering the same text once, unstreamed**. Shipped (2 review rounds). Fence rule verified against every attack (closer with info string, `~~~` vs backticks, tab indent, open-at-3/close-at-0 both ways, never-closed fence, inline spans); ordered-list numbering measured, not assumed — glamour renders a lone segment from its own literal number, so `3.` stays `3.`; the self-healing property holds byte-identically across 15 fixtures × 3 chunk sizes, including setext, front matter, split indented code, CRLF and the phantom fence. Round 1 found a real allocation regression (620 MB of garbage to stream a 20 KB reply, because the whole ANSI-heavy prefix was re-joined per token) — fixed by caching the joined prefix and rebuilding it only on flush, landing at ~1.2x base bytes; also fixed: blank lines between flushed paragraphs vanished mid-stream and popped back at finalization (this was most of the hitch, and prose now has none), whitespace-only and CRLF blank lines were not treated as blank so CRLF never engaged at all, an unreachable `streamWraps` fallback plus a comment describing callers that do not exist, and a missing content-shrink guard its sibling `streamWrap.render` already had. F3's fence-indent question went to the owner as a genuine trade and he chose the degrade-not-garble side — see F15. | M | done |
| 6 | **Per-project sessions.** Sessions are not global — they are keyed on the exact cwd (`session.DefaultPath`), which is the actual defect: `repo/` vs `repo/sub/` vs each git worktree land in separate pools, so `/resume` loses sessions. Derive the key from the git toplevel (resolving a linked worktree to its main repo via `--git-common-dir` so worktrees share one pool), fall back to abs cwd outside git. Record `ProjectRoot` in the session header, migrate existing dirs by their recorded `Header.CWD`, default listings to the current project with an `--all` escape hatch. **Merged.** `gitinfo.ProjectRoot` resolves a linked worktree back to its main repo; `session.ProjectKey` uses it (falling back to abs cwd outside git) to key `DefaultPath`/`SessionDir`. `migrateLegacyDirs` moves old exact-cwd session directories into the new project-keyed one by peeking each legacy dir's recorded header CWD, leaving unrelated projects and already-gone directories untouched. `tyci session list`/`/resume` default to the current project; `--all` (CLI and TUI) lists everything. Reviewed independently — worktree resolution, migration safety (including the concurrent-process and deleted-directory edge cases), and the `--all` wiring were all tested by hand, not just read; no blocking issues. | S | done |
| 4 | **Message a running subagent.** The mechanism exists for the main agent (`agent.Config.NextMessages`, drained after every `runOnce`) and is deliberately nil for children (`btw.go:173`). Add a per-job mailbox to `jobs.Registry` (`Post`/`DrainMessages`), wire it through the `tools` adapter pattern using `JobIDCtxKey`, expose `/msg <job> text` plus a `message` tool so the parent model can steer its own children. Delivery is between model turns, not mid-tool. **Merged.** Mailbox lives on `jobs.Job`/`Registry` behind the existing mutex; `Registry.Resolve` accepts full or short `#N` ids. `tools.JobMailbox`/`JobMailboxNextMessages` mirror item 25's `JobActivityToucher` pattern and feed `cfg.NextMessages`, wired into both `agentRunner.run` (every subagent child, sync or async) and `/btw`'s job config — so it reaches background jobs, not just synchronous ones. `message` joins `subagentDeniedTools` (a plain child can never have its own child to message); main agent and `/btw` keep it. Reviewed independently, including an end-to-end test through real production wiring (async job blocked on `ask_parent`, message posted, next model call carries it) — no blocking issues. | S | done |
| 5 | **Session forking.** Start a child from an arbitrary point in the current history. `btw.go:154 forkMessagesForBtw` already deep-copies history and `session.sanitizeMessageSequence` already repairs a cut that lands inside a tool-call/result pair. Address the fork point by transcript index (live) or session event id (persisted); ship fork-as-background-child first, fork-as-new-session second; add `inherit_history` to the subagent tool. **Merged.** `ForkAtIndex`/`ForkAtEventID` address a live transcript or a persisted session by the same event-id scheme `WriteCompaction` already writes; both repair a mid-tool-call/result cut via the exported `SanitizeMessageSequence` plus a new, deliberately separate `dropTrailingUnansweredToolCalls` (ordinary `/resume` must keep a trailing unanswered tool call as crash-recovery state — folding the two together broke a pinned existing test). `ForkChildJob`/`ForkNewSession` reuse the real job registry/`resumable` map and the real session-file/list path, not parallel mechanisms; `inherit_history` on `subagent` stamps the live conversation into tool-call context (`connector.WithConversation`) and seeds the child's history end-to-end. Reviewed independently with real jobs, real session files, and a `-race` check of the async-goroutine read path; one non-blocking note (an unstated no-in-place-mutation invariant `WithConversation` relies on) documented in a follow-up commit before merge. | S–M | done |
| 3 | **`/btw` → fork as session.** `/btw` stays read-only (it asks "is this worth doing?" and must not modify anything). When the answer is yes, the side-conversation is promoted to a real subagent, and the main thread is told a new subthread exists and that it should wait for it. Depends on #4 and #5. **Merged in `b5c1242`**: read-only gate/schema, one-shot promotion with full transcript, child runtime isolation, bounded/timeout evaluation lifecycle, mailbox/resumable handling and parent wait handoff. | M | done |
| 2 | **`@agent:` in completions.** One list offering both files and agents: picking a file inserts `@file:<path>`, picking an agent inserts `@agent:<name>` and instructs the model to continue that work in a subagent. The engine already supports agent selection (`subagent`'s `agent` field, `.tyci/agents/*.md`) — this is the missing UI. **Merged.** The popup ranks files and `agentdefs.List` agents together; picking an agent inserts `@agent:<name>`. Turned out the old `@file:` prefix never actually existed (a bare path was inserted) — added now, for symmetry, with the two affected tests updated. `conductor.extractAgentMentions` scans the submitted message for `@agent:<name>` and appends a separate harness-framed instruction block (not touching the original text) telling the model to continue via `subagent`; deliberately does not pre-validate the name against `agentdefs.List` since `runSingleTask` already fails loudly on an unknown one. Reviewed independently — no blocking issues; noted non-blocking gaps (no render-level test for an agent entry with a description, and a narrow false-positive risk if `@agent:` appears inside a code fence/quote) left as-is. | S | done |
| 1 | ~~**Right-side sidebar**~~ **Merged (3 review rounds).** Toggleable, with tabs: Tokens, Sessions, Bash, Lua. Clicking the context figure in the status bar opens the Tokens tab. The detail that used to sit in the status bar is already rendered by `display.buildUsageDetail` and is waiting for a place to live. Bash and Lua tabs need per-run ledgers (command/script, start time, state) — the point is watching long-running work. Add a **Subagents** tab too, rendered as a **tree with per-agent token counts** — the shape asked for is `main 10k` with `subagent 1 5k` and `subagent 2 10k` indented under it, and `subagent 3 15k` indented one level deeper under subagent 2 — plus what each child produced and a way to re-enter the conversation at that point. **One tree, not a live list beside a history list** — a finished child is the same row it was while running, just in a terminal state, because the reason to open this tab is usually to reach a child that has already finished. **Tokens are per-agent and do not roll up; cost does** — decided deliberately. Tokens are not additive across models in any useful sense (10k on a child's cheap model is not the same quantity as 10k on the parent's expensive one), while cost is the one figure every model shares, and summing it is already what the ledger does for the status bar. So a parent row shows its own tokens next to a cost that includes everything beneath it, which also keeps the root row agreeing with the status bar. **Reuse the status bar's convention for an unpriced child** (`+?` when part of the total has no price, `display/tui_tokens.go`): a rolled-up cost that silently omits an unpriced descendant understates the bill, which is the one thing `internal/ledger` was built not to do. What must stay unmistakable is which rows are still live. The states already exist (`jobs/job.go:8`: `running`, `waiting_answer`, `done`, `failed`, `truncated`), so this is a rendering decision rather than new bookkeeping. `waiting_answer` is the one state that must never read as history: it means a child is blocked on the user, so it belongs at the top and stays undimmed.  **Three things that do not exist yet, checked in the code:** (i) there are no per-child token counts — `ledger.Row` (`internal/ledger/ledger.go:41`) is keyed by `{kind, provider, model}`, so every child on the same model collapses into one `subagent` bucket; the fix is on the existing path rather than a new one, since `ledger.Watch` already wraps the Sink per model call, so the job id joins the key there. (ii) there is no parent link — `jobs.Job` (`jobs/job.go:15`) has an `ID` and no `ParentID`, and the registry is flat, so there is nothing to indent from; the parent's id is already in the context at spawn time (`JobIDCtxKey`), it is simply not recorded. (iii) the tree is **one level deep until item 21 lands** — `subagent` is denied to children, so the grandchild in the sketch cannot exist yet. (iv) **the registry cannot tell a subagent from a backgrounded shell command** — `jobs.Job` has no kind field, and the same registry holds both (the retention comment in `jobs/registry.go` is explicitly about a backgrounded bash command's 256 KiB output). So this tab has nothing to filter on today, and the Bash tab needs the same distinction from the other side; one field on `Job` serves both. (v) **history here is bounded and process-local by definition** — `maxRetainedTerminalJobs` is 50 finished jobs (running and waiting ones are never pruned), and nothing survives a restart, so this is "this session, last 50". Say so in the tab rather than letting the user hunt for a child the registry already dropped. **Depends on item 17:** a child with no job id cannot be a row in this tree, and the fallback path has none. Most of that already exists but only the model can reach it — `tools/resume.go` continues a finished job's exact conversation as a new job, backed by the `resumable` transcript map in `main.go:59-79`. Two things to sort out: that map is never pruned (a long session keeps every child's full transcript in memory, which is the same class of leak as the conversation history), and it is keyed by job id, so children taking item 17's fallback path have no entry. Invasive in the render path (`display/tui_view.go` builds full-width rows; caches are width-keyed; mouse hit-testing must clamp to the main column), so it goes last. **Rendering decision — REVERSED by the user:** the sidebar must NOT be a separate overlay window that covers the main window. Instead it must contain the main window with the conversation content alongside it, so both parts (sidebar + main conversation) are visible at once as a side-by-side column layout. [Historic note: it was first shipped as a full-screen right-anchored overlay (like the jobs/todo/btw modals) to reuse the existing width-keyed cache/mouse-clamp machinery untouched, documented in `display/tui_sidebar.go`'s package comment — that overlay approach has now been superseded; the sidebar should be reworked to a live side-by-side column. The width-keyed cache + mouse-clamp-hit-testing-to-main-column work this was originally avoiding (see `display/tui_view.go`) becomes scope.] **Tab navigation — REVERSED by the user:** while the sidebar is active, `Tab`/`ShiftTab` must NOT be used to move between the sidebar's tabs — `Tab` also cycles focus in the terminal (which is the root of a problem once the sidebar is up), so navigation between the sidebar's tabs must be arrows only (Left/Right). Drop the `case tea.KeyTab` / `case tea.KeyShiftTab` switch-tab bindings in `updateSidebar` (`display/tui_sidebar.go`) and keep only Left/Right (and the existing mouse click on a tab label); decide whether Tab/ShiftTab should pass through to their normal (switch-model) handling instead. Shipped: `jobs.Job` gained `Kind` (subagent/bash/cron) and `ParentID`; `internal/ledger`'s row key gained a job id (`ledger.UsageByJob`), with `ledger.ByModel()` added back for the Tokens tab's per-model aggregation; `buildSubagentTree` walks `ParentID` to N levels generically (not hardcoded to today's depth-1 limit), rolls up cost with the status bar's `+?` unpriced convention, sorts `waiting_answer` first/undimmed, and handles orphaned/cyclic parent chains without duplicating or hanging. Toggle: Ctrl+T or clicking the status bar's context figure. Three review rounds; fixes included a test data race (snapshotting a live `*Job` instead of `Wait`ing), tab-click hit-test/renderer offset mismatch, missing scroll (plus a stale-scroll-after-resize follow-up), a Tokens-tab per-job-instead-of-per-model regression from the ledger key change, an unbounded `backgroundJobs` display mirror, `/btw`/fork/resume jobs recording zero ledger usage (rendering as free), and a `$?` that discarded known-priced dollars in a partially-priced aggregate. **Deferred to Inbox, not blocking:** F2x-class chained-orphan ordering, an untested stale-scroll path outside the resize trigger, doc-comment inaccuracies, and double tree-build per render frame — see Inbox. **Rework merged (2 review rounds):** the sidebar is now a live side-by-side column (`mainColumnWidth`/`sidebarColumnWidth` are the shared source of truth; `renderFrame` joins a narrowed main render with the sidebar via `lipgloss.JoinHorizontal`; width-keyed caches invalidate on toggle/resize), and navigation is a `sidebarFocused` state machine: focus starts on the conversation, Ctrl+Right walks into the sidebar's tabs, Ctrl+Left/Ctrl+Right walk back out from any tab (plain Left/Right at the first/last tab still walk back out too), and while sidebar-focused the prompt does not receive keystrokes. Tab/ShiftTab no longer switch sidebar tabs — they fall through to the normal model-switch behavior regardless of focus. Round 1 found and round 2 confirmed the fix for one real bug: streamed model output (`tuiMsgBlock`) was silently dropped whenever the sidebar had focus, which could also leave `m.reading` stuck false — fixed by forwarding it to `handleBlockMsg` the same way `tui_modal.go` already does for the older subagent modal. | M–L | done |
| 17 | **Every child gets a job id, in every mode.** Prerequisite for items 15, 16, 4 and the sidebar's Subagents tab, and much smaller than it first looked: blocking children in an interactive session are *already* registered before their first iteration (`tools/subagent.go:596-599`, and the comment at `:64-69` says this is deliberate — "so ask/report_progress work there too, for free"). The gap is only the fallback at `tools/subagent.go:352-358`, taken when `jobStarter == nil` or `!backgroundAllowed(ctx)` — that is `--print`/`tyci run` (`main.go:30`) and every child-of-child (`backgroundAllowed` returns false whenever `SubagentSinkCtxKey` is set, `tools/bgbash.go:92-97`). Those drop to plain `runTasks` with no id, so `report_progress`, `ask` and `resume` all refuse. Fix: register them through `jobStarter.Start` as well, skipping only the 60s handoff, not the registration — `jobStarter` is wired in every mode (`main.go:424`). Retention is already bounded (50 terminal jobs, `jobs/registry.go:239-249`) and inline finishes already suppress the finish notice (`tools/subagent.go:604-612`), so nothing downstream needs changing. Second half of the job: the error text in `tools/progress.go` and the copy in `providers/provider.go:111` and `tools/help_articles.go` still say "only works inside an async background job", which is already false for the sync path — the model reads that and believes it. **Review round 2 found three things before this can merge:** (a) HIGH — giving these children a job id makes `ask` pass its gate on exactly the path where no answer can ever arrive, so a child that asks a question now blocks for the full 600s timeout and then dies, where before it failed instantly and carried on; `ask` needs a separate signal from "has a job id" (e.g. a ctx flag set only by modes that actually drain `JobNotices`). (b) MEDIUM — the "job is terminal before Run returns" claim is a race: `close(done)` happens inside `fn`, while the registry sets status and closes `job.done` only after `fn` returns, so two of the new tests fail under `-count=1000`; wait on the registry's own completion signal instead of inventing a second one. (c) MEDIUM — the child-of-child motivation is **fiction**: `subagentDeniedTools` blocks `subagent` in both the child's schema and its runtime gate, so grandchildren do not exist in this codebase. The only reachable path is `--print`/`tyci run` plus item 20's fallthrough bug, and three of the five new tests pass with the fix reverted. Rewrite the justification and the tests around the path that is real.  **Merged.** Three review rounds. Landed: the `--print`/`tyci run` fallback registers through `jobStarter.Start` instead of dropping to plain `runTasks`; `runRegistered` was folded into `runWithHandoff` behind a `handoff bool` rather than kept as a second wait loop — the copy had silently dropped the `ctx.Done()` arm, so Ctrl-C during `tyci run` left `Run` parked on the child until the 600s backstop; `ask` gates on a new `AskUnroutableCtxKey`, set only where no answer can ever arrive, so it fails fast instead of stalling 600s; `cancelRemaining` stops the children and deliberately does not claim them as handed, which would have emitted a FAILED notice nothing in that mode drains. **Not** closed by this item, despite the original premise: `resume` still needs a `job_id` the blocking path never surfaces to the model (`resultsToToolResult`), so only `report_progress` became usable. Review round 2 found the cancellation payload had zero coverage — `_ = st.cancel` passed the entire suite — and round 3 found the notice leak; both now ship with tests verified to fail when reverted. | S | done |
| 16 | **A finished child says how to come back to it.** When a subagent ends, append a line to the result telling the parent it can continue that exact conversation later — the `resume` tool already does this, backed by the transcript map in `main.go:59-79`, and today the model has to know that on its own. The precedent is the worktree note (`tools/subagent.go:961-983`) which already tells the parent which branch to look at. The value is the follow-up question: the user asks "and what about X?" an hour later, and the parent can hand the work back to the child that already has the context instead of starting over. **The note must not lie:** it needs a real job id. Blocking children in an interactive session already have one (`tools/subagent.go:596-599` registers before the first iteration), so this covers nearly everything; the exception is item 17's fallback path, where there is no id and so no note. Keep the wording short; it is appended to every child's output and the parent pays for it in tokens. **Merged.** Item 17 already closed the "no id" gap for every real invocation — `jobStarter` is wired unconditionally in `main.go`, and `tools/subagent.go`'s `spawn`/`runWithHandoff` register a real job id through it for both the async and blocking-in-every-mode paths; the only jobID-less path left is the direct, unregistered `runTasks` call reached solely when there is no job registry at all, which is test-only. So the normal-completion case in `main.go`'s `agentRunner.run` now appends the same `resumeHint(jobID)` item 28's cutoff path already used (it no-ops when jobID is empty), reusing the helper as-is rather than duplicating its wording. Three existing wiring tests (`TestWiring_R1/A8/Q1`) asserted exact job-result text and needed updating to include the new suffix; new tests pin the hint appearing with the correct job id, its absence when no job id exists, that it stays one short line, and that the cutoff and normal-completion hints never both fire for the same completion. | S | done |
| 15 | **A subagent must report in every N iterations.** The plumbing exists and is voluntary: `report_progress` (`tools/progress.go`) posts a note onto the job (`jobs/registry.go:222`), surfaced by `wait` and the jobs panel — and a child that never calls it is silent for ten minutes. Make it periodic and enforced: the agent loop knows its iteration count, and `agent/agent.go:290-322` already injects harness reminders as user messages (todos, pending jobs), so the same machinery can inject "you are N iterations in, post one line on where you are" every 20 — with the same max-per-turn discipline so it cannot crowd out the real conversation. Fire-and-forget by design: the parent never has to answer, it just stops being blind. **Decided (see item 17 for the shared prerequisite):** the reminder is **time-based, not step-based** — prompt for a note at the next iteration boundary when more than 60s have passed since the child's last one, reusing `SubagentBackgroundAfterSec` as the constant. Iterations vary from one second to ten minutes, and `MaxIterations` defaults to unlimited (`agent/agent.go:31`), so both a flat count and a fraction of the cap are meaningless. Temporal settled this argument for activity heartbeats years ago for exactly this reason. Trap: track "time since last note" on the `Job` (extend `jobs.Job`), not in the agent loop — a child that never reports must not be nagged every single iteration. Pairs with item 4, which is the opposite direction. | S–M | todo |
| 25 | **An active job shows how long since its last sign of life.** The jobs panel line today is `⟳ #3 running <desc> (42s)` (`display/tui_jobs_panel.go:106` `formatJobLine`, duration from `jobDuration:88`) — elapsed since `StartedAt` and nothing else, so a child that went quiet two minutes ago is indistinguishable from one streaming tokens right now. Add, for **running** jobs only, the time since the job last did something. **Signal:** the real heartbeat is the `SubagentSink` — `Text`/`Thinking`/`ToolCallStart`/`ToolCallDelta`/`ToolCallEnd` all pass through `streamingCollector` (`tools/subagent.go:239`), which already knows its own job id via `JobIDCtxKey`; touch there, not in the tools. Backgrounded bash needs nothing new — `tools/bash.go:403` already calls `SetProgress` per output line, so route `SetProgress` through the same touch. Do **not** derive this from `Job.Progress` alone: `report_progress` is voluntary and rare, so every subagent would read as permanently idle. **Storage:** a plain `int64` of unix nanos on `jobs.Job`, read/written with `atomic.LoadInt64`/`StoreInt64` — not `atomic.Int64`, because `Snapshot` (`jobs/job.go:49`) copies the struct by value and `go vet`'s copylocks would reject it. A per-delta `r.mu.Lock()` is not acceptable: that mutex is shared by every job and the whole registry. `Snapshot` materialises it into a `LastActivity time.Time` on the returned value. Set it at `Start` (`jobs/registry.go:53`) so a job that has done nothing yet reads as idle-since-start, and never clear it on completion — `Progress` already persists past terminal for the same reason, and the Ctrl+B modal wants "went quiet 4m before it failed". **Render:** only past a threshold, so a busy child's line does not rewrite on every tick and thrash the width-keyed render cache — 5s is the suggested cut, and the wording must read as "last sign of life", not "hung": a single legitimate 3-minute test run is silent and fine. **Order:** the mechanism does not need item 17, but 17 is what gives `--print`/`tyci run` children ids at all, and both edit `tools/subagent.go` — land 17 first. Feeds item 1's Subagents tab, which needs exactly this column. **Merged.** `Job.lastActivity` (plain `int64`, atomic, CAS-guarded against going backward), touched from `streamingCollector`'s five sink methods and bash's `SetProgress`, via a lock-free `Registry.TouchActivity` that only takes `r.mu` for the map lookup. Panel line adds "quiet Xs" past a 5s threshold, running jobs only. Reviewed independently (mutation-checked including the CAS logic, `-race` clean on the touched packages). | XS–S | done |
| 26 | **Stop a running subagent — it, its background commands, everything under it.** There is no kill switch for a subagent today, in any mode. `kill_job` (`tools/bgbash.go:290`) looks *only* in `bgRegistry`, so passing it a subagent id answers "job %q is not a running background command"; and `jobs.Registry.Start` (`jobs/registry.go:47`) accepts a ctx but never stores a cancel, so nothing outside can stop a job at all. The subagent's own cancel exists but is a local in `spawn` (`tools/subagent.go:636`), reachable only from the `defer` inside its own goroutine. Esc deliberately leaves detached children running, and `commands.go:486`'s "runTUI kills any survivors on exit" is bash-only — a subagent outlives the session that spawned it. **Four pieces.** (1) `Registry` stores each job's cancel and grows a `Cancel(id)`; that alone kills the child and its **foreground** bash, which run under the job ctx. (2) It does **not** kill the child's *backgrounded* bash: `tools/bash.go:238` uses `context.WithoutCancel` on purpose, and nothing records which job started a given background command — so `bgRegistry` needs a parent job id, and a cascade that cancels the subtree before the job itself. This is the part the user actually asked for and the only genuinely new plumbing. (3) `kill_job` stops being bash-only: dispatch on the job's kind. `jobs.Job` has **no kind field** — subagent jobs and backgrounded-bash jobs are indistinguishable in the registry — and item 1's Subagents tab needs that field anyway, so add it here. (4) A user-facing way in: the Ctrl+B panel already lists jobs and `display/tui_jobs_panel.go:98` `shortJobID` displays them as `#3`, so whatever the user types must accept that short form, not the full `job-<nanos>-<n>`. **Decide:** a killed job currently lands as `failed` with a `context.Canceled` error. Recommend keeping `failed` with an unmistakable error string rather than adding a sixth status — a new status touches `jobStatusIcon` (`tui_jobs_panel.go:71`), the panel, the modal and every status switch in the tests for little gain — but the panel must read as "stopped by you", not as a crash. **Safety:** the model can call `kill_job`. A child must be able to kill only what it started (its own subtree); only the main agent may kill anything. Enforce that from the parentage in (2), not from trust — otherwise one confused child can kill the whole session's work. **Scope note:** "its children" today means its background commands only, because `subagentDeniedTools` means grandchildren do not exist; item 21's `scout` would add a real second level, so build the cascade as a subtree walk now rather than a one-level sweep. Verify on the way that a killed `isolation:"worktree"` child still runs `finishWorktree` (`tools/subagent.go:887`) — a `defer` on a cancelled ctx does run, but the cleanup itself uses `context.WithoutCancel` (`:1054`) and wants a test. | S–M | done |
| 31 | **A subagent can set its own wall-clock timeout; the fixed default is 600s.** `SubagentTimeoutSec` (`tools/subagent.go:24`) is a package constant — same dead timer for every child, and 600s is a long guess given `MaxIterations` is unlimited. A caller should be able to shorten it: add an optional `timeout` (seconds) field to the `subagent` tool schema, wired into `spawn`'s `context.WithTimeout` (`tools/subagent.go:761`). Decide bounds deliberately: a floor (~120s, so a short request cannot race straight into item 28's deadline warning), a ceiling, and precedence (explicit beats the default). Pair with item 28's 45s-before-deadline warning so a short explicit timeout still gets the "last step, summarise now" message before the deadline, and with item 21: `scout` should inherit the caller's *remaining* time rather than re-arm its own. Added 2026-08-23 (user feedback) after observing a long-running child hit the default repeatedly; reviewed in item 26, where the child-timeout wall-clock model lives. | S | todo |
| 27 | **`ask` is invisible to the person and answered by the model instead.** Reported from use: "I don't see questions anywhere in the TUI, and the LLM keeps thinking we answered." Three separate causes, all confirmed in the code. (A) A question makes the job **vanish** from the one always-visible place: `runningBackgroundJobs()` (`display/tui_jobs_panel.go:59`) keeps only `StatusRunning`, and `Registry.Ask` (`jobs/registry.go:157`) switches the job to `StatusWaitingAnswer` — so the line drops off the inline panel at exactly the moment it needs attention, and comes back only once answered. `jobStatusIcon` (`:71`) has no case for it either, so it would render as the grey `?` default. The panel also never shows `Job.Question`, which is populated and unused there. (B) **A human cannot answer at all.** Nothing in the TUI or the REPL calls `Registry.Answer`; the only caller is the `answer` *tool*, which only the model can invoke. Fix is a slash command, and it must accept the short id the panel actually prints — `shortJobID` (`:98`) shows `job-<nanos>-<n>` as `#3` — with a bare `/answer <text>` working when exactly one job waits. (C) The model answering on the user's behalf is **written into the prompts on purpose**: the notice at `main.go:452` and `buildJobReminder` (`agent/agent.go:376`) both tell the model to call `answer(...)` now. So the parent invents an answer, the child receives it as bare `Content: answer` with no provenance (`tools/ask.go`), attributes it to a person, and the parent's summary honestly reports "we answered". Fix: the registry records who answered and `ask` marks a non-human answer as "another agent, NOT the user"; the notice and reminder change from "answer it" to "relay it to the user unless you genuinely know" — keeping the true part, that a blocked job makes no progress and its work is discarded on timeout. Agent-to-agent answering stays legal and deliberate (see `AnswerTool`'s doc comment): what changes is the default advice and the provenance, not the capability. **Merged** (3 review rounds; round 3 fixed the status-bar overflow with a general `truncateStatusText` clamp in `buildStatus`, made the composition root testable via a startup panic when `onAnswer` is nil, and split the job reminder's wording on a new `agent.Config.Interactive` flag so non-interactive modes stop promising a `/answer` nobody can type). **Deferred to item 29 — do not lose it:** cause (C) is fixed in only 2 of 6 model-facing places, and `/answer` is still undiscoverable. See item 29. | S | done |
| 30 | **User-reported: the transcript viewport pads with blank lines that are not really there, leaving the newest content stranded well above the status bar instead of flush against it.** Screenshot from a real session: a finished assistant turn (ending in a horizontal rule) sits at the top of the message region, then several dozen blank rows, then the status line (`↑12 lines`, i.e. scrolled up from bottom) and the input box. If the person is genuinely scrolled up with more real content below, that gap should be filled with that content, not blanks — so `totalRenderedLines()`/`buildViewportRows()` (`display/tui_scroll.go`, `display/tui_render_buffer.go:147`) is undercounting the real line total relative to what `getBlockLines` actually renders for the tail blocks, and `viewport-pad` rows (`tui_render_buffer.go:181-186`) are filling in for lines that exist but were not counted. **Ruled out so far** (read, not fixed): `forceRenderDirtyBlocks` (`display/tui_markdown.go:111`) already calls `m.invalidateTotalLines()` unconditionally at its top before re-rendering thinking/text/error/block kinds, so the three `cachedLineCount = 0` sites inside it (`:139`, `:155`, `:168`) are not silently stale on their own. The streaming-hot-path incremental total update in `tui_blocks.go` (~`:276-320`, the `oldCount`/`newLines` delta) and the flush-to-scrollback path (`tui_scrollback.go`) also read as internally consistent on inspection. Needs actual reproduction — build a test around `TestStreamingPinsToExactBottom` / `TestTuiModel_ViewAtBottomShowsTailOfLongStreamingBlock` (`display/tui_perf_test.go`, `display/tui_scroll_test.go`) that streams a block whose raw line count differs a lot from its final glamour-rendered line count (headers/code blocks/bullets typically *add* lines), finalizes it, and asserts `totalRenderedLines()` matches `len(m.buildAllFlatRenderLines())` and that `buildViewportRows` at a `↑12`-style scroll offset contains no `viewport-pad` rows when the transcript is actually longer than the viewport. Also worth checking: whether the bug needs a *second* streaming block/turn after the first finalizes (i.e. does the stale total from turn N survive into turn N+1's math), since the screenshot shows this well into an ongoing session, not on the very first message. Queued to start after item 28. **Merged.** Real cause: after a resize, `invalidateAllBlockLineCounts()` deliberately leaves a *flushed* block's `cachedLineCount` untouched (re-wrapped lazily on next view), but `totalRenderedLines()` only re-derived a block's count when it was exactly `0` — never zero for an already-flushed block — so it kept summing the OLD-width count while `buildAllFlatRenderLines`/`buildFlatRenderLinesInRange` always re-wrap correctly via `getBlockLines`. Fix: `totalRenderedLines()` now also treats a flushed block as stale when `flushedWidth != m.width`. The streaming/glamour-finalization hypothesis was tried and ruled out with its own tests. Reviewed independently (revert-confirmed, `-race` clean). One deferred nitpick: `blockAtVisibleLine`'s identical fallback pattern (`tui_scroll.go:167-210`) has the same gap but is unreachable outside tests — left as-is. | S–M | done |
| 29 | **Finish item 27's deferred half: stop telling the model to answer for the user everywhere, and make `/answer` discoverable.** Item 27 fixed the notice/reminder wording in 2 places but left 4 model-facing strings still telling it to answer(...) as if it were the only channel: `tools/wait.go:143`, `tools/help_articles.go:104` and `:255`, `tools/subagent.go:772`, and the subagent schema at `tools/tool.go:250`. Reword each the same way item 27 did: relay to the user (or genuinely-known info) unless it truly knows the answer. Also fix discoverability: no `/help` command in the binary, README/docs never mention `/answer`, no slash-command completion, and the jobs panel line does not say how to reply to a waiting job. Overlaps [[F18]]/[[F19]]. **Scope added 2026-08-22 (user feedback):** the tool names themselves are part of the confusion — a model reading `ask`/`answer` in isolation has no cue that the other end might be a human, another agent, or unreachable, and the current descriptions ("Ask the parent one question...", "Answer a job that is blocked on ask...") are vague enough to say almost nothing. Rename the two model-facing tools: `ask` → **`ask_parent`**, `answer` → **`answer_job`** (matching the existing `_job`-suffix convention from `kill_job` in item 26, and the `job_id`-bearing style of `wait`/`resume` — NOT `subagent_*`, which would be wrong since these fire from `/btw` side-conversations too, not only from `subagent`-spawned children). Update every reference: the tool registry map key (`tools/tool.go:640`), the schema `name` fields, `subagentDeniedTools`/allowlists, all five prose strings above, `providers/provider.go:110`'s `toolLines`, tests, and docs. Rewrite both descriptions to actually say something: who "the parent" can be (the agent or person that spawned this job — could be a human at the keyboard, another agent, or nobody reachable at all if no handoff is available), what happens on timeout (work discarded), and for `answer_job` — that it should be used to relay a real answer, never to invent one for a human who hasn't actually replied. **Scope added 2026-08-23 (user feedback, confirmed live: asking a running tyci session "what do ask and answer do" got back a hedging, role-blind answer):** both tools are handed to every agent regardless of whether they can ever actually work for it, which is the real source of the confusion, not just wording. `GetToolsSchema()` (`tools/tool.go:117`) — used for the top-level main agent — includes `ask` even though the main conversation has no job id and `AskTool.Run` (`tools/ask.go`) will always fail it immediately ("ask only works inside a job"); `ask` is only ever meaningful for something that *is* a job (a subagent child, or a `/btw` side-conversation). Symmetrically, `GetSubagentToolsSchema()` (`tools/tool.go:526`) filters out `subagent`/`agents` for a depth-1 child but leaves `answer_job` in — yet a plain child that cannot spawn `subagent` can never have a descendant blocked on `ask_parent`, so `answer_job` is dead weight for it too; only something holding the `subagent` tool (today: the main agent, and `/btw`) can ever have a real use for `answer_job`. Gate both schemas so each agent is offered only the tool that can possibly do something for it: drop `ask_parent` from `GetToolsSchema()`'s top-level set (or make it conditional the same way item 27 keyed `agent.Config.Interactive` — the same role signal may apply here), and drop `answer_job` from `GetSubagentToolsSchema()`'s filtered set alongside `subagent`/`agents`. Verify `/btw` still gets both (it inherits the main config's full tool set per F3/item 21's design note) and that this doesn't regress once item 21's `scout`/nested-subagent work lands, since a depth-2+ agent that *can* spawn further children would then need `answer_job` back. **Scope changed 2026-08-23 (user decision):** dropped the "make `/answer` discoverable" half entirely — the human-facing `/answer` slash command (`display/tui_keys.go`, `tui.go`'s `answerFunc`, `tui_api.go`'s `requireAnswerWiring`/`onAnswer` wiring, `commands.go`'s `tuiAnswerFunc`) is **removed**, not documented, on the judgment that nobody will type it when the model already relays a plain message to a blocked job via `answer_job`. Remove the command, its wiring, its startup-panic guard, its tests (`interactive_answer_test.go`, `tui_api_answer_wiring_test.go`), and the jobs-panel hint text that referenced it (`tui_jobs_panel.go` — revert to describing the waiting state without instructing a now-nonexistent command). A blocked job is answerable only via the model calling `answer_job`, typically after relaying a person's plain-text reply. **Merged.** Renamed everywhere, reworded both descriptions and the five "answer it now" sites, gated `ask_parent` out of the top-level agent (kept for `/btw`) and `answer_job` out of a plain subagent child, and removed `/answer` and all its wiring/tests cleanly. Reviewed independently (mutation-checked, `-race` clean). **Non-blocking, noted for awareness:** `AskTool.Run`'s "[another agent answered this, NOT the user]" prefix is now unconditional on every live path (`fromUser` can never be true with `/answer` gone) — an honest label since an agent did mediate the relay, but worth a deliberate look if it turns out that wording reads wrong once a human's answer is faithfully relayed. | S | done |
| 28 | **A subagent gets told its last step is its last, and the parent gets told how to continue it.** Two halves, both missing today. **(A) Warn the child.** The loop at `agent/agent.go:142` just stops; `:331` prints "possible infinite loop. Stopping." and returns `ErrMaxIterations`. The child is cut off mid-thought and its partial work is whatever happened to be in `msgs`. Fix: on the iteration before the cap, inject a harness-authored message — same shape as `buildTodoReminder`/`buildJobReminder` (`agent/agent.go:368`), framed as an automated check and not as the user — saying this is the last step and to summarise now. **Critical detail:** at the last iteration the model produces exactly one more assistant turn, and if that turn is a tool call the loop exits before the model ever sees the result. So the message must say explicitly: do not call tools, write the summary as text. **(B) Tell the parent how to continue.** `main.go:335` already appends `[note: subagent stopped at its %d-iteration limit; the result above may be incomplete]` — with no mention of `resume`. And the hint would be unactionable as written: `ResumeTool.Run` (`tools/resume.go:36-41`) requires `job_id` as *input*, and the blocking path never surfaces an id to the model at all (`resultsToToolResult`, `tools/subagent.go:456-471` — found by the item 17 review). So this item must ship the job id **with** the note, or the advice is a dead end. Also fix the sibling branch at `main.go:330`: a child that hits the cap with zero output returns a hard error advising "narrow the task or split it", even though `resumable[jobID]` **was** already stashed (`:316`'s condition passes for truncated regardless of text) — the conversation is resumable and the message says otherwise. **(C) The part that actually matters most.** `DefaultSubagentMaxIterations` is `-1` (`tools/tool.go:59`), so the iteration cap only ever fires when someone sets one. The real terminator is the **600s wall clock** (`SubagentTimeoutSec`, `tools/subagent.go:24`), and there the child gets no warning at all, the result is a plain failure with no partial content (`runSingleTask` ~`:1012`), and **no resumable entry is stashed** — `main.go:316` requires `err == nil || truncated`, and a deadline error is neither. Ten minutes of work discarded with nothing to resume. Extend the same mechanism to the deadline: before each iteration, if the remaining deadline is under a threshold, inject the same last-step message, and make the deadline path stash a resumable entry and carry the partial text the way the truncation path does. This half is harder than (A) and (B) — "how many seconds is one more turn" is a guess — so pick a deliberately generous threshold and say why. **Merged.** A 45s-before-deadline threshold (commented, same order of magnitude as `SubagentBackgroundAfterSec`) injects a harness message forbidding tool calls, for both the iteration-cap and wall-clock paths; the wall-clock timeout now stashes a resumable entry with partial text via a new `tools.ErrSubagentTimedOut` sentinel; the parent's note now carries a real `resume(job_id=..., task=...)` hint, including in the zero-output case that previously claimed non-resumability. Reviewed independently (mutation-checked, `-race` clean). | S–M | done |
| 14 | **Model variant / thinking mode.** There is no way to ask for extended thinking or set its budget. `connector.Request` carries only `Temperature` and `MaxTokens` (`connector/message.go:41`); no connector sends a thinking or reasoning parameter — Anthropic wants `thinking.budget_tokens`, OpenAI-shaped APIs want `reasoning_effort`, Gemini wants `thinkingConfig`. So the field has to reach `Request`, each connector maps it its own way, and the catalog should say which models support it. Then the UI: a way to pick it per session (the model picker or a `/think` command), per named agent (frontmatter, next to `temperature`), and per subagent call. **The catalog already answers this, and we throw the answer away.** Verified against a live `https://models.dev/api.json` fetch (7246 models, 193 providers): each model publishes `reasoning` (bool), `reasoning_options` (a list), `tool_call` (bool), plus `structured_output`, `temperature`, `attachment`, `modalities`, `family`, `knowledge`, `status`. `reasoning_options` comes in three shapes — `{type:"toggle"}` (622 models, on/off with no levels), `{type:"effort", values:[...]}`, and `{type:"budget_tokens"}`, often combined. **The effort level lists differ per model** and are not a fixed vocabulary: `[low,medium,high]` (577), `[low,medium,high,xhigh,max]` (199), `[minimal,low,medium,high]` (196), `[none,high]` (143), `[high,max]`, and more — so a hardcoded low/medium/high picker would be wrong for most models; read the list from the catalog. But `ModelsDevModel` (`internal/connect/modelsdev.go:29`) keeps only `id`, `name`, `cost`, `limit`, so every refresh discards all of it — confirmed against the cached file, whose models carry exactly those four keys. This is the **same defect item 18 fixed for `cost`/`limit`**, and it was only noticed there because missing prices were visible. Carry `reasoning`, `reasoning_options` and `tool_call` through the cache first; the UI work below depends on it. **Related, and a user-visible bug of its own: nothing filters on `tool_call`.** 1083 of the 7246 models have `tool_call: false` and cannot function in tyci at all, yet the model picker has no field to exclude them by — so it will happily offer a model that can never call a single tool. Caveat for this setup: models.dev does not carry the hand-added `nexos` provider, so its flags would have to come from the gateway's own `/v1/models`, if it publishes them at all — check before assuming the picker can filter uniformly. Note the related known bug — every converter currently *discards* thinking blocks on the way back in (docs/architecture-refactor.md:1022), so turning thinking on without fixing that buys tokens and shows nothing. | M | todo |
| 23 | **Trusted-directory list, asked once.** Prerequisite for item 22 and the answer to F6. A project-local `.tyci` can carry code tyci will execute — `hooks.json` runs `sh -c` today, Lua tools run shell through `ctx.run`, and item 22 would add a local `mcp.json` able to launch any binary. So the first time tyci runs in a directory that contains any of that, it asks once whether this project is trusted, and remembers the answer. **Not** a per-tool permission prompt — that is rejected for this project and stays rejected; this is one question about one directory, closer to an editor's workspace trust. Design: the list lives in `~/.tyci/trust.json` and is global by definition (a project must never be able to declare itself trusted); the key is the project root, so reuse whatever item 6 settles on for deriving it, and record the decision against the git toplevel so subdirectories and worktrees inherit it. **Ask on the first run in every directory**, whether or not it currently holds a project-local `.tyci` — decided deliberately over "ask only when there is something to trust". The trade is a question in directories where nothing would have been executed, in exchange for two properties worth more: the answer is already on record when a `.tyci` appears later (a `git pull` that adds one must not change behaviour silently), and the user can always tell whether the directory they are in is trusted, instead of that depending on a file they would have to think to check. Untrusted is not a refusal to run: the session proceeds normally with project-local hooks, Lua tools and MCP servers ignored, and says so once rather than silently behaving differently. Everything else about the session is unaffected — the model, the built-in tools and the global config all work exactly as usual. Non-interactive modes (`--print`, `tyci run`, cron) must never block on a question: they treat unknown as untrusted and print one line. Re-ask when the trusted content changes materially, or accept that trust is per directory and not per content — pick one deliberately and write down why. **Merged.** `internal/trust` keys `trust.json` on item 6's `session.ProjectKey`; policy is per-project, not per-content (documented reasoning: re-hashing hooks.json/every `.lua` file for a guarantee an attacker could trivially route around isn't worth the machinery). Wired into `commands.go`'s `initCommon` — the one funnel every mode (TUI, console, `run`/`--print`, and cron via `tyci run`) goes through — before any local hooks/Lua load, gating `.tyci/hooks.json` and `.tyci/tools` while global `~/.tyci/` content always loads regardless. No project-local `mcp.json` exists yet (item 22 still open), so nothing to gate there. Fails closed on a corrupt/unreadable `trust.json` (verified live against the real binary, then pinned with a test before merge) — never fails open. Reviewed independently with real manual runs: non-interactive never blocks, an interactive yes persists and is honored by later non-interactive runs, a linked worktree of an already-trusted repo inherits trust without re-prompting. | S | done |
| 22 | **Project-local config overriding the global one, consistently.** Half of this already works, and the half that does not is arbitrary. Local today: `hooks.json` (both files load, `internal/hooks/hooks.go:219`), `agents/` (local wins over global), Lua `tools/`, and `memory/`. Global only: `config.json` (`agent/config.go` — `default_model`, `favorite_models`, `max_tokens`, `prompt_cache`), `model.json`, `mcp.json`, `cron.json`, and `skills/`. So a repo can bring its own agents and hooks but cannot say "this project uses this model" or "this project needs this MCP server", which are the two most obviously per-project things in the list. Decide the semantics per file rather than one blanket rule: `config.json` should merge key by key (a local file naming only `default_model` must not wipe the global `favorite_models`), `mcp.json` and `skills/` should probably union, `agents/` already replaces by name. **Decided: `cron.json` also becomes per-project** — a local `.tyci/cron.json` merges with the global one (same semantics as the other per-project files, not global-only). Precedence: CLI flag > local > global. Also decide what `tyci` does when it is run outside any project — the lookup must not walk up past a git root and pick up a stranger's `.tyci`. **Merged.** `config.json` merges field-by-field (a local file naming only `default_model` no longer wipes global `favorite_models`/`max_tokens`/`prompt_cache`); `model.json`, `mcp.json`, and `skills/` union by name/key with local winning on collision (mirroring how `agents/` already worked); `cron.json` unions jobs by name, local wins, and the runner actually executes local-defined jobs and writes `LastRun`/`LastStatus` back into whichever file defines that job. `mcp.json` and `cron.json` are trust-gated end-to-end — verified the trusted bool reaches the runtime layer (MCP connect, cron tick), not just the merge/display layer: an untrusted project's local servers never connect and its local jobs never run. None of the five files walk up past cwd looking for `.tyci` (matching the pre-existing hooks.json/agents/ mechanism, which never did either), so the "stranger's .tyci" concern doesn't arise. Reviewed independently, including mutation-testing the four highest-risk mechanisms (field-merge, both trust gates, write-back routing) — all caught by an existing test when reverted. | S–M | done |
| 21 | **Grandchildren — a crippled "scout" that may nest to depth 4.** Decided: full recursion is not wanted, but depth 3-4 is. That is safe for a scout and unsafe for a `subagent`, so the two get different limits: **depth 1 stays the full `subagent`** (jobs, async, worktree, named agents), and **depth 2 through 4 may only spawn scouts**, which nest freely inside that range. Depth alone is not the safety property — a scout is safe at depth 4 for the same reasons it is safe at depth 2: it registers no job, so the flat notice/answer channel, `PendingLines` and the 50-job retention never see it; it is synchronous and cannot detach, so it dies with its caller; and it **inherits its caller's remaining deadline**, so a chain converges on its own instead of nesting 180s inside 180s inside 600s. What actually bounds the burn at depth 4 is the concurrency semaphore and the per-turn budget below, not the depth number. Do NOT lift `subagentDeniedTools`. A general `subagent` at depth 2 breaks the notice/answer model in user-visible ways, and buys little over flat fan-out. Instead add a separate tool with a deliberately narrow profile: `scout` — one task string, synchronous, read-only tools (`read, grep, glob, ls, bash` read-only, `help, lua`), `MaxIterations` 15 (not the unlimited default), 180s **inheriting the child's remaining deadline**, no `async`, no `isolation`, no `agent`/`model` choice, and no job of its own. Depth lives in the context (`tools.DepthCtxKey` set by `runSingleTask` next to `SubagentSinkCtxKey`), the gate becomes depth-derived rather than the flat denial at `main.go:360` and `:399` — `subagent` allowed at depth 0 only, `scout` at depths 1-3, nothing at depth 4 — and `agents` stays denied at every depth. **Cost control, in order of value:** a concurrency semaphore — this is the primary control once depth reaches 4, not a secondary one (2 scouts per caller, 6 process-wide — `runTasks` has no cap at all today and `parseTasks` does not bound `len(tasks)`); a per-turn delegation budget read from `internal/ledger` (deny past +$3, hard stop +$6, add a `Scout` kind so the bar can show it); the depth cap last, since alone it controls nothing: depth 4 with two scouts each is 16 leaves, and the semaphore is what keeps that from being 16 concurrent models. **Blockers, in order:** item 20 must land first (nesting multiplies the double-run — and item 17 removes the `backgroundAllowed` guard that accidentally protects the nested level today, so 17+20 in the wrong order plus nesting is 4× model spend and 4× file writes); `runAsync` (`tools/subagent.go:623`) checks only `jobStarter != nil`, never `backgroundAllowed`, so an async grandchild would detach with `context.WithoutCancel`, outlive its parent, and post finish notices and `ask` questions to the **top-level** agent about a job it never spawned; terminal-job retention is 50 process-wide and would evict ids a parent still holds; `resumable` is unbounded; worktree-in-worktree branches off the child's branch and hands the parent a diff against a base it never saw. And there is **no kill switch anywhere** — nothing in `jobs` cancels a job, and Esc deliberately leaves detached children running — which must exist before any nesting ships. | M | todo |
| 7 | **`agent.lua`** — a Lua-scripted conversation process for repeatable tasks. `internal/workflow/engine.go` is a complete orchestration engine with `tyci.run_tool/new_session/resume_session` and session `prompt/await/save`, and has no caller outside its own test. Wire a CLI entry point and `.tyci/agents/*.lua` discovery, then close the real gaps: sessions get no tools and no schema, `MaxIterations` is hardcoded to 10, and there is no `subagent`/`wait` sugar for fan-out. | M | todo |

| 50 | **Nexos fallback opt-out in provider/model DSNs.** Preserve an HTTP query option from the model/provider URI through request construction, initially to support Nexos Chat Completions' `fallbacks=false` (query parameter, not a header/body field). Merge it with existing DSN options such as `api=responses` and `reasoning=...` without dropping or duplicating keys, URL-encode correctly, and add request-URL tests for the supported API paths. The option must be usable by the generated Pi/OpenCode Nexos configuration, and the implementation must document that this controls Nexos' server-side fallback only — tyci/OpenCode/Pi application-level fallback lists remain separate. | S–M | todo |

## Done

| # | Item |
|---|------|
| — | Git branch in the top bar, resolved correctly inside linked worktrees (`internal/gitinfo`). |
| — | No blank line inside a run of tools and thinking: the spacer rule is now one shared helper (`display/tui_render_buffer.go` `spacerAfter`) instead of the same condition duplicated in both flat-line builders, where a disagreement would have shifted every later line index and sent mouse selection to the wrong row. |
| — | Status bar right side reduced to context used + session cost including subagents. Backed by `internal/pricing` (costs and context limits from the models.dev catalog) and `internal/ledger` (process-wide spend, main separated from delegated). |
| 19 | **Thinking collapsed by default, like a tool.** A thinking block now renders as one collapsed line — `thinking(Find all usages of validateOptions…) 42ms - click to display` — and opens the same modal a tool block does (`tui_mouse.go`, `tui_modal_content.go`). Timing: a thinking block starts its own clock in `newContentBlock` (`display/tui_blocks.go`) since `block.duration` is only ever set by `tool-end`, and `forceRenderDirtyBlocks` freezes it — plus `toolState = "done"` — at the same point that already finalizes a block (next text/tool block, or `kind: "done"`). Summary: `freezeThinkingSummary` (`display/tui_render_thinking.go`) decides the one-line summary from the block's opening words once there is enough text to fill the truncation width, then never touches it again, so streaming deltas cannot make the collapsed line flicker; a placeholder ("…") stands in until that happens. The block no longer renders its full text inline at all (`renderThinkingBlock` replaces the old glamour/dim-text path in `tui_render_block.go` and `tui_markdown.go`); the full text still reaches the modal because it's read straight from `block.content`, untouched. Fixed on the way: `gutterLen`'s "thinking" case (`tui_selection.go`) still assumed the old bare "│ " gutter — copying a collapsed line would have included the "thinking " label. |

## Inbox — found while working, not yet triaged

Format: `F<n> | what, file:line | why it matters | found by`.

| F | What | Why it matters | Found by |
|---|------|----------------|----------|
| F1 | `resume` zachowuje stary callback mailboxa (`btw.go:132-164`, `main.go:310-332`) | Wiadomość wysłana do nowego job ID po resume zostaje zakolejkowana, ale wznowiony child drenuje mailbox poprzedniego joba i jej nie zobaczy. | review diagnostyczny message |
| F2 | `kill_job` nie anuluje joba w `StatusWaitingAnswer` (`jobs/registry.go:288-292`, `:374-381`) | Child zablokowany na `ask_parent` może pozostać waiting bez końca; `message` nie odblokowuje ask. | review diagnostyczny message |
| F3 | ~~Tasks/sidebar hit-test używa indeksu renderowanej linii jak indeksu joba.~~ **Naprawione.** Kliknięcie liczyło `row` (indeks linii po scrollu — poprawny) ale porównywało go z `sidebarRowCount()`, które dla zakładki Tasks zwraca liczbę JOBÓW, nie liczbę renderowanych linii (nagłówki "Subagents"/"Bash"/"Lua" i syntetyczny root dokładają linie bez dokładania jobów). Efekt: klik na któryś z późniejszych wierszy Bash/Lua (gdy jego indeks linii przekraczał samą liczbę jobów, mimo że job tam realnie był) był po cichu ignorowany. Drugi błąd w tym samym bloku: `line := row + m.sidebarVisibleScroll(layout)` doliczał scroll PO RAZ DRUGI, mimo że `row` już go uwzględniał — psuło mapowanie przy niezerowym scrollu. Naprawa: `display/tui_sidebar.go`'s `updateSidebar` liczy `line` raz, ogranicza go do `len(sidebarTaskRows(width))` (rzeczywista liczba linii, nie jobów), po czym szuka najbliższego joba na/za tą linią przez `sidebarTaskJobRows` — dokładnie ten mechanizm "linia → wiersz" zamiast zakładania równości. Regresja `TestSidebarMouse_TaskClickPastJobCountBound` (`display/tui_sidebar_test.go`) buduje 5 jobów Bash za dwoma nagłówkami/rootem tak, by ostatni job wypadł na linii 7 przy `sidebarRowCount()==5`, i potwierdza że stary kod go odrzucał (FAIL po `git stash` naprawy), a nowy poprawnie otwiera. | analiza sidebara |
| F4 | `runtime.Goexit()` w funkcji joba omija terminalizację (`jobs/registry.go:123-140`) | Job pozostaje running, `Wait` może wisieć i nie ma completion notice; osobny problem poza zakresem #38. | review #38 |
| F5 | Item 10's own spec calls for an auto-compact threshold ("Needs `/compact` ... and an auto-compact threshold"); this pass only ships the model-facing budget reminder, not an automatic compaction trigger. | The model can ignore the reminder indefinitely and keep paying full price/quality cost past the threshold with nothing forcing a compaction. | item 10 review |
| F6 | `tyci session export --markdown` does not exist; item 10 says the markdown renderer "gives this for free," but `sessioncmd.go` has no such subcommand. | A user cannot get the searchable dump for a session on demand — only compaction produces one, and only for sessions that get compacted. | item 10 review |
| F7 | Subagent children get no `session.Session`/JSONL at all (`main.go:320-331`), so there is no child markdown file to write and nothing for a parent's dump to link to — item 10 says "a child's file is written when the child finishes." | Deliberately not implemented this round (would require giving every subagent its own session file, a materially larger change); the parent-links-to-child-markdown half of item 10's design stays unfinished until that lands. | item 10 review |
| F8 | `LoadForReplay` (`session/session.go:1353`) does not call `SanitizeMessageSequence`, unlike `RebuildMessages` (`session/session.go:778`), which does. | `LoadForReplay` is what feeds the model on `/resume` (`tui_mode.go:91`, `interactive.go:313`); a corrupt/truncated JSONL cut mid tool-call/result pair can produce an orphan tool result and a provider 400 on resume. | item 10 review |
| F9 | `refreshMarkdownDump` (`session/session.go:274`, `:316`) re-parses and rewrites the entire derived markdown dump on every single `WriteMessage`/`WriteCompaction`/`WriteSessionEnd` call. | O(n²) over a session's length, on the agent's hot path — a long session pays an ever-growing full-file rewrite for every message. | item 10 review |
| F10 | A fork/resumed-fork child carries the **main conversation's** `Compactor` and gets `compact` in its schema with no owner check (`fork.go`/`btw.go:349` nil `Session`/`NextMessages`/todos but not `Compactor`; `btwConfig` sets `Schema = GetAllToolsSchemaJSON()` which includes `compact`, `tools/tool.go:541`; `agent/run_once.go:25` installs `cfg.Compactor` into the child ctx; `CompactTool.Run` has no owner check; neither `ForkChildJob` nor `jobResumerAdapter.Resume` installs a tool gate, unlike `Promote`'s `DenySubagentRecursion` and `startBtw`'s `BtwReadOnlyGate`). | A fork child calling `compact(summary=...)` compacts the **user's live main conversation** — silent, destructive, and reachable from a resumed child too. Same stashed-config-carries-parent-state class as F1. | batch 1 review (out of scope, verified in code) |
| F11 | ~~Cztery niezabezpieczone globale hakow zadan.~~ **Naprawione.** `jobExtensionRequester` (`tools/extension.go`), `jobCanceler` i `jobLister` (`tools/killjob.go`), `jobPromoter` (`tools/btw_readonly.go`) dostaly ten sam wzorzec, ktory tura 1 (B2) dala pozostalej siodemce: `sync.RWMutex` plus getter kopiujacy pod `RLock`, nigdy nie trzymajac blokady w czasie wywolania w interfejs. Koder znalazl miejsce czytania, ktorego nie bylo w moim zleceniu: `AnswerTool.Run` (`tools/ask.go:186`) tez czyta `jobExtensionRequester`. Recenzent znalazl ostatnie: `tools/extension_test.go:47` czytal surowy global, choc pisal przez setter — dokladnie ta niedopasowana para. **Zweryfikowane, nie zalozone:** cofnalem po kolei kazdy z czterech do niezabezpieczonej wersji i uruchomilem jego test — kazdy dal po 2 `WARNING: DATA RACE`, z zabezpieczeniem zero. Pelne pokrycie sciezki produkcyjnej ma tylko `jobLister` (`TestParentIDOf_...`), i tak to jest teraz opisane w komentarzu, zamiast obiecywac wiecej. Poprawiony tez nieprawdziwy komentarz przy `jobPromoter`: `promote_btw` jest w `subagentDeniedTools`, wiec NIE jest czytany z goroutine zadania — uzasadnieniem jest druga polowa rozumowania, nie pierwsza. Bez zmian w zachowaniu produkcyjnym. Pozostale globale tej klasy: F24, F25. | zrobione |
| F12 | ~~`TestInitCommon_UntrustedProject_SkipsLocalHooksAndLua_KeepsGlobal` nie jest `-count>1` czysty.~~ **Naprawione.** Przyczyna: `initCommon` woła `tools.LoadAndRegisterLocalLuaTools` dla ZAUFANEGO projektu, a `toolRegistry` to procesowa mapa, ktorej nic nie sprzata — wiec kazdy test przechodzacy przez `initCommon` na zaufanej atrapie zostawial `local-trust-wiring-tool` zarejestrowany dla wszystkiego, co po nim. W drugiej iteracji test o nieufnym projekcie widzial go i slusznie protestowal. Winowajca nie byl bezposredni sasiad (tak zakladal komentarz w pliku, i to wlasnie kazalo pierwszej probie naprawy chybic), tylko `TestInitCommon_TrustedProject_CronLocalDirSet`. Naprawione w `newInitCommonTestCmd(t)`, przez ktore przechodza wszystkie testy `initCommon`, wiec nowy test nie moze zapomniec. Nowy `tools.SnapshotLuaToolsForTesting`. **Kod produkcyjny bez zmian — wina byla w tescie.** | zrobione |
| F13 | ~~`renderWidth()` is not idempotent under the sidebar shadow model.~~ **Naprawione.** Zweryfikowane najpierw: real `width=120` daje `renderWidth()=71`, a ręcznie zbudowany shadow (`width=71, sidebarActive=true`) daje `mainColumnWidth()=34` — komentarz "the answer is identical from either" był po prostu fałszywy, bo `mainColumnWidth()` nie ma jak odróżnić "to już jest zwężona szerokość" od "to jest prawdziwa szerokość terminala"; oba wyglądają tak samo (jakieś `m.width` + `sidebarActive=true`). Naprawa w `display/tui_view.go`'s `renderFrame`: shadow model (`mainM`) dostaje `mainM.sidebarActive = false` zaraz po ustawieniu `mainM.width = m.mainColumnWidth()` — skoro `mainM.width` jest już finalną, zwężoną wartością, dalsze zwężanie ma się nie odbywać. Zweryfikowane, że nic innego w ścieżce renderowania nie czyta `sidebarActive` z `mainM` (jedyni czytelnicy to `mainColumnWidth`/`renderWidth`), więc zgaszenie flagi jest bezpieczne. Zaktualizowany też fałszywy komentarz przy `renderWidth()` (`tui_sidebar_view.go`). Testy: `TestRenderWidth_DoubleNarrowIsARealFootgun` dokumentuje sam błąd arytmetyczny, `TestRenderFrame_MainColumnWrapsAtSingleNarrowWidth` jedzie przez prawdziwy `renderFrame()` i sprawdza, że zawinięte linie mieszczą się w `mainColumnWidth()` — na starym kodzie blok zawijał się w 7 linii zamiast oczekiwanych ≤4 (podwójne zwężenie), na nowym przechodzi. **Niezależna recenzja (Opus) mutacyjnie znalazła DRUGI hand-rolled shadow model** (`display/tui_sidebar.go`'s `routeSidebarMsg`, obsługa `tea.MouseMsg` dla kliknięć w głównej kolumnie przy otwartym sidebarze), który kopiował ten sam wzorzec (`mainM.width = m.mainColumnWidth()`, `sidebarActive` zostawione `true`) — dokładnie ten sam podwójny-zwężacz, tylko GORSZY: `getBlockLines` pisze przez `m.blocks[idx]`, slice dzielący backing array z prawdziwym modelem, więc podwójnie zawężony wrap na zimnym cache'u (np. przy scrollowaniu kółkiem myszy) zostaje w cache'u NA STAŁE, nie tylko na jedną klatkę. Naprawa: wydzielona `mainShadow()` (`display/tui_sidebar_view.go`) — jedyne dozwolone miejsce budowy takiego shadow modelu (`width = mainColumnWidth()`, `sidebarActive = false`) — użyta w OBU miejscach (`tui_view.go`'s `renderFrame` i `tui_sidebar.go`'s `routeSidebarMsg`); przy okazji przywrócone `result.sidebarActive = m.sidebarActive` po stronie `routeSidebarMsg` (bo `mainShadow()` gasi flagę, którą trzeba przywrócić na wyniku, inaczej klik w głównej kolumnie po cichu zamykałby sidebar). Test `TestSidebarMouse_MainColumnDispatchDoesNotDoubleNarrowCache` (`display/tui_sidebar_test.go`) otwiera sidebar przy width=120, zostawia blok z zimnym cache'em, wymusza `clampScroll` przez scroll kółkiem myszy w głównej kolumnie i porównuje scache'owane linie z pojedynczym, poprawnym zwężeniem — na starym kodzie 4 linie zamiast 2 (podwójne zwężenie, FAIL), na nowym zgadza się. | item 51 review |
| F14 | ~~`handleResize` sets `m.width` immediately, but `invalidateAllBlockLineCounts` only runs on `handleResizeFlush` ~100 ms later.~~ **Naprawione.** `display/tui_update.go`'s `handleResize` teraz woła `m.invalidateAllBlockLineCounts()` natychmiast, zamiast czekać na debounce'owany `handleResizeFlush`. Inwalidacja tylko czyści liczniki/mapy (`cachedLineCount`, `streamWraps`, `mdStreamState`, `mdCacheRendered`) — realne przewijanie/re-wrap dzieje się leniwie przy najbliższym odczycie, więc jest tania i nie musi dzielić debounce'a z drogą częścią (`clampScroll`, `painter.repaint()`), która zostaje w `handleResizeFlush` jak było — szybki resize-drag nadal nie przerysowuje całego ekranu przy każdej klatce. `handleResizeFlush` dostał dodatkowo tę samą inwalidację w gałęzi "resize doszedł między wywołaniami" (obronnie, bo w normalnym torze `m.width` i tak już zgadza się z `resizeWidth` zanim flush odpali). Test `TestHandleResize_InvalidatesWidthCachesImmediately` (`display/tui_scroll_test.go`) renderuje bold-owy blok przy width=80, robi `Update(WindowSizeMsg{Width:30})` i — BEZ czekania na flush — sprawdza że `cachedLineCount` już jest wyzerowany i że żadna linia z `buildAllFlatRenderLines()` nie przekracza `renderWidth()==30`; na starym kodzie `cachedLineCount` zostawał nietknięty (test FAIL), na nowym przechodzi. **Niezależna recenzja (Opus) zmierzyła to jako regresję ~37x latencji** (200 bloków: 1.3ms → 49ms na cały cykl `Update()+View()`): `invalidateAllBlockLineCounts` ustawia też `m.cachedTotalLines = -1`, a `totalRenderedLines()` z nieważnym cache'em przechodzi przez WSZYSTKIE bloki, wołając `getBlockLines` dla każdego z zerowym licznikiem — i, co gorsza, dla KAŻDEGO sflushowanego (wypaged'owanego) bloku `stale := b.flushed && flushedWidth != renderWidth()` jest prawdą natychmiast po zmianie `m.width`, więc każdy z nich wraca z dysku przez `ensureBlockResident` na KAŻDYM zdarzeniu resize podczas przeciągania, nie raz na gest. Naprawa: nowa `invalidateDirtyBlockWidthCaches` (`display/tui_scroll.go`) zamiast pełnego `invalidateAllBlockLineCounts` w `handleResize` — czyści `streamWraps`/`mdStreamState`/`mdCacheRendered` i `cachedLineCount`/`cachedLines` TYLKO dla bloków aktualnie streamowanych (`m.dirtyBlocks`, zwykle 0 lub 1), świadomie NIE dotykając `cachedTotalLines` ani skończonych/sflushowanych bloków — to jedyne miejsce, gdzie stara-szerokość linia mogła się realnie pojawić przed osadzeniem debounce'a; pełny `invalidateAllBlockLineCounts` (wraz z `cachedTotalLines=-1`) wraca do `handleResizeFlush`, bezwarunkowo (usunięty błędny warunek `if m.width != m.resizeWidth`, pod którym poprzednia wersja tej poprawki przypadkiem schowała całą inwalidację — `handleResize` zawsze synchronizuje `m.width`/`resizeWidth`, więc ten warunek prawie nigdy nie był prawdziwy). Fałszywy komentarz "tanie, bo tylko czyści liczniki/mapy" poprawiony na dokładny opis kosztu. Testy zaktualizowane: `TestHandleResize_InvalidatesWidthCachesImmediately` teraz zostawia blok streamującym (nie `"done"`) — to jest jedyny przypadek objęty natychmiastową inwalidacją; nowy `TestHandleResizeFlush_InvalidatesFinishedBlockCaches` dokumentuje, że skończony blok czeka na flush; nowy `TestHandleResize_DoesNotInvalidateTotalLinesWhenIdle` przypina wprost, że `cachedTotalLines` przeżywa resize bez streamowania (na starym kodzie z tej tury FAIL, `-1`; na naprawionym zostaje ważne). | item 51 review |
| F15 | Known accepted artifact of item 51: a lone ``` inside a 4-space- or tab-indented code block opens a phantom fence that never closes (`display/tui_markdown_segments.go:56-78`), so progressive rendering silently stops for the rest of that message. Reproduced: `"Example:\n\n    ```\n    some code\n\nafter blank\n…"` leaves `inFence=true` forever with `safeUpto` stuck at 10 of 66 bytes. **Ponownie zweryfikowane (2026-08-26), pozostawione bez zmian — to nie jest błąd do naprawy.** Odtworzone krok po kroku przez `mdStreamState.scan()` na dokładnie tym przykładzie: linia `"    ```"` ma `indent=4` (niecapowany), więc `fenceLineInfo` łapie ją jako otwarcie fence'a mimo że semantycznie jest to zwykły 4-spacjowy blok kodu; żadna kolejna linia w przykładzie nie zamyka fence'a (brak dopasowanego markera), więc `inFence` zostaje `true` do końca treści — `safeUpto` zamraża się na końcu `"Example:\n\n"` (10 bajtów), reszta streamuje się jako surowy tekst aż do `forceRenderDirtyBlocks` przy zakończeniu bloku. Sprawdzone też, że alternatywa (cap indentu ≤3 wg CommonMark) naprawdę psuje gorzej niż koryguje: capping powoduje, że fence zagnieżdżony w liście (indent >3 w surowym tekście, patrz F3 z przeglądu itemu 51 — dokładnie dlatego indent jest niecapowany) staje się niewidoczny dla skanera, więc blank line WEWNĄTRZ takiego fence'a zostaje błędnie potraktowana jako bezpieczny punkt flush — to faktycznie GARBLE'uje wyjście (kod renderuje się jako proza, `#`→H1, `-`→bullet), podczas gdy obecne zachowanie tylko DEGRADUJE (zatrzymuje progresywne stylowanie, ale surowy tekst zostaje poprawny i samo-uzdrawia się przy finalizacji bloku). Degradacja jest strukturalnie bezpieczniejsza niż garbling, więc trade-off nadal się broni — zostawione bez zmian kodu. | item 51 review |
| F16 | ~~Two low-severity leftovers in item 51's composer.~~ **(a) Naprawione, (b) zweryfikowane i pozostawione bez zmian.** (a) Zweryfikowane najpierw, że inwariant BYŁ fałszywy w ogólności (choć praktycznie nieosiągalny przez naturalny streaming — patrz niżej): `streamWrap.render` (`display/tui_render_block.go:52-54`) ma własne `if out == "" { lines = []string{""} }` — treść złożona wyłącznie z bajtów `\n` zawija się do `out=""`, ale `lines` wciąż ma długość 1. `len(tailLines) > 0` NIE dowodzi więc `tailWrapped != ""`. Naprawa w `display/tui_markdown_segments.go`'s `renderStreamingMarkdown`: `default` branch nie ufa już `tailWrapped` samo w sobie — najpierw sprawdza `len(lines)==0` (to jest jedyna rzecz, której pustość faktycznie musi odzwierciedlać ten string), a gdy `lines` niepuste ale `tailWrapped==""`, zwraca `st.renderedPrefixJoined` (już scache'owane, zero dodatkowego kosztu) zamiast fałszywego `""`, które `getBlockLines` odczytałoby jako "nic do renderowania" i wyzerowałoby dopiero co ustawione `cachedLines`. Test `TestStreamWrapRender_CanReturnEmptyOutWithNonEmptyLines` przypina fakt niskopoziomowy; `TestRenderStreamingMarkdown_NonEmptyPrefixSurvivesBlankTail` (stan `mdStreamState` ustawiony ręcznie, bo naturalny `scan()` zamiata taki "czysto-nowoliniowy" tail od razu do `safeUpto` — to jest brzegowy przypadek kontraktu typu, nie coś co scan() sam produkuje) potwierdza, że stary kod zwracał `""` mimo niepustego stylowanego prefiksu (FAIL po cofnięciu poprawki), nowy zwraca niepusty string. (b) Zweryfikowane w kodzie: `fenceLineInfo` liczy indent znakowo (tab = 1), więc `fenceIndent+3` dla fence'a wciętego tabem jest niespójne kolumnowo. Pozostawione bez zmian — udokumentowane wprost w kodzie (`display/tui_markdown_segments.go`, komentarz nad `fenceLineInfo`): (1) cała reszta skanera liczy wcięcie znakowo, nie kolumnowo, więc kolumnowa poprawka tylko tutaj byłaby niespójna; (2) taby we wcięciu fence'a są rzadkością w markdown generowanym przez LLM (jedyne realne źródło treści tutaj); (3) niedoszacowanie szerokości taba zawęża tolerancję, czyli obciąża w stronę "fence nie zostanie rozpoznany jako zamknięty" — ten sam bezpieczny degrade co F15, nie groźniejszy kierunek fałszywego zamknięcia/garblingu. **Poprawka po niezależnej recenzji (Opus):** punkt (3) był prawdziwy TYLKO dla taba przy otwierającym fence'ie — dla taba przy ZAMYKAJĄCYM jest odwrotnie: otwieracz `` ``` `` (indent 0, próg 3) + zamykacz `"\t\t```"` liczy się jako indent 2 ≤ 3 i ZAMYKA fence, mimo że wizualnie (≈16 kolumn w zagnieżdżonej liście) to wciąż powinien być kod — czyli kierunek przedwczesnego zamknięcia/garblingu, dokładnie ten, który komentarz błędnie twierdził że nie może się zdarzyć. Decyzja (zostawić bez zmian) się broni — oba kierunki są rzadkie i przypadek trzeba trafić jednocześnie na obu końcach fence'a — ale komentarz w kodzie skrócony i poprawiony, by nie twierdzić jednokierunkowego obciążenia, którego nie ma. | item 51 review |
| F17 | ~~`TestLuaToolRun_RecordsHistory` nie jest `-count>1` czysty.~~ **Naprawione.** Test czytal stan wyjsciowy i sprawdzal `stan+2`, co jest prawda tylko dopoki bufor `luaRuns` ma miejsce: po nasyceniu na `maxRetainedLuaRuns = 50` dwa nowe wpisy wypychaja dwa stare, zamiast powiekszyc licznik. Nowy `tools.SnapshotLuaRunHistoryForTesting` czysci historie na czas testu. Dowiedzione, nie zalozone: przy sztucznie nasyconym buforze test bez poprawki daje dokladnie zgloszony objaw (`expected 52 history entries, got 50`), z poprawka przechodzi. | zrobione |
| F18 | `display/tui_input.go:99-107` — komentarz nad wysyłanym `KeyPgUp` opisuje mechanizm, który w bubbles v1.0.0 nie istnieje: textarea zeruje viewport KeyMap przy `New()` i nie mapuje KeyPgUp w swoim switchu, więc syntetyczny PgUp jest noopem; kursor pozostaje widoczny wyłącznie dzięki końcowemu `repositionView()` w `Update`. Zachowanie poprawne (potwierdzone probem), ale komentarz wprowadza w błąd i sygnalizuje zależność od nieistniejącej ścieżki. | Myląca dokumentacja dokładnie tam, gdzie ktoś będzie szukał wyjaśnienia scrollowania inputu. Jednolinijkowy cleanup komentarza. | item 52 review |
| F19 | `inputWrappedRows()` (`display/tui_input_wrap.go`) kosztuje O(n) na keystroke → O(n²) na sesję pisania. Zmierzone (M4 Pro): ~143µs @1k znaków, ~1.5ms @10k na wywołanie (1–3 wywołania/klawisz). Dla promptów <2k znaków niezauważalne; worst case 10k znaków pisanych znak po znaku ≈ kilkanaście sekund agregowanego CPU na sesję. | Akceptowalny edge case; tania optymalizacja gdyby kiedykolwiek bolała: cache `(len(value), width) → rows` na TuiModel, pomijanie przy niezmienionej długości. | item 52 review |
| F20 | ~~Przejściowa niespójność szerokości inputu przy otwarciu sidebara.~~ **Naprawione.** Sprawdzone po F13: to NIE rozwiązało się samo przy okazji zmiany shadow modelu, bo `mainM.input.SetWidth(...)` w `renderFrame` zawsze operował tylko na kopii `mainM.input` — prawdziwe `m.input` nigdy nie było tam dotykane, niezależnie od `renderWidth()`. Naprawa w `display/tui_sidebar.go`: `openSidebar` i `closeSidebar` teraz same wołają `m.input.SetWidth(max(10, m.mainColumnWidth()-2))` na PRAWDZIWYM modelu, symetrycznie, w momencie przełączenia — nie trzeba czekać na kolejny klawisz ani na kopię z `renderFrame`. Test `TestSidebarToggle_SyncsRealInputWidth` (`display/tui_sidebar_test.go`) otwiera/zamyka sidebar i porównuje `m.input.Width()` z wartością uzyskaną przez przepuszczenie tej samej `SetWidth` przez scratch-kopię textarei (bo `textarea.SetWidth(w)` nie ustawia `Width()` na dokładnie `w` — rezerwuje miejsce na prompt) — na starym kodzie `m.input.Width()` zostawał szeroki zaraz po `openSidebar` (FAIL), na nowym zwęża się natychmiast. **Niezależna recenzja (Opus) znalazła, że to naprawiało tylko szerokość, nie wysokość** — `SetWidth` NIE przelicza liczby wierszy, robi to osobno `capInputHeight()` (`display/tui_input.go`), którego ani `openSidebar`, ani `closeSidebar` nie wołały; ~200-znakowy prompt przy width=120 renderował się z 2 wierszy zamiast potrzebnych 4 zaraz po otwarciu sidebara — dokładnie zgłoszony objaw, tylko po stronie wysokości. Dopisane `m.capInputHeight()` zaraz po obu wywołaniach `SetWidth`. Ta sama wada w `display/tui_update.go`'s `handleResize`/`handleResizeFlush`: obie wołały `SetWidth` PEŁNĄ szerokością terminala (`msg.Width`/`m.resizeWidth`) nawet przy otwartym sidebarze, więc każdy resize cofał całą naprawę F20 z powrotem do pełnej szerokości — zmienione na `m.mainColumnWidth()-2` (m.width jest już przypisane nad obydwoma wywołaniami) plus `capInputHeight()` w obu miejscach. Nowe testy: `TestSidebarToggle_SyncsRealInputHeight` (otwiera/zamyka sidebar z 202-znakowym promptem dobranym tak, by liczba wierszy faktycznie się różniła między pełną a zwężoną szerokością — na starym kodzie FAIL, wysokość zostaje przy 2 zamiast 4) i `TestHandleResize_KeepsInputNarrowedWhileSidebarOpen` (resize przy otwartym sidebarze, sprawdza że `m.input.Width()` zostaje zwężone do `mainColumnWidth()` zarówno po `handleResize`, jak i po `handleResizeFlush` — na starym kodzie FAIL, szerokość skacze do pełnej 136 zamiast 79). | item 52 review |
| F21 | ~~`TestWiring_D5_ResidualMailboxSweepIsCapped` przewraca sie na `main` 4 razy na 5.~~ **Naprawione.** Sweep wola `notices.Notify` osobno dla KAZDEJ wiadomosci, a test oprozznial kolejke i przerywal przy pierwszym niepustym wyniku — lapal 3 z 6 w polowie zapisu, a `Drain` usuwa to, co zwroci, wiec reszta przepadala. Test scigal sie z wlasnym przedmiotem badania. Teraz zbiera wyniki przez kilka oprozznien, a potem po chwili oprozznia jeszcze raz — to drugie jest konieczne, bo samo osiagniecie oczekiwanej liczby nie dowodzi, ze sweep sie na niej zatrzymal. Sprawdzone: przy sztucznie zdjetym limicie test wykrywa 8 zamiast 6, a pelny pakiet daje 5/5 zamiast 1/5. Ten sam wzorzec utwardzony w blizniaczym tescie C3. **Kod produkcyjny bez zmian.** | zrobione |
| F22 | Nazwany agent, którego `tools:` nie zawiera `ask_parent` (`tools/toolgate.go:158`, `GetSubagentToolsSchemaJSONFor` w `tools/tool.go:750`), dostaje wspólny prompt dziecka, który po turze 3 twierdzi wprost, że `ask_parent` "is a real tool" — dla niego nieprawda. | To punkt 41 z odwróconym znakiem: sprzeczność wróciła, tylko z drugiej strony. Wcześniej prompt tylko wymieniał narzędzie, teraz zapewnia, że jest prawdziwe, więc szkoda jest większa niż przed poprawką. Wymaga znajomości whitelisty w chwili budowania promptu. | recenzja tury 3 |
| F23 | `renderProgressHistory` (`tools/wait.go`) dolicza do budżetu znak nowej linii, który `TrimRight` potem usuwa — blok wychodzi zawsze ≤ budżet−1. | Kosmetyczne i po bezpiecznej stronie (nigdy nie przekracza budżetu), ale model kosztu opisuje coś minimalnie innego niż wynik. Zostawione świadomie: naprawa zmienia liczby w testach granicznych bez zysku dla czytelnika. | recenzja tury 3 |
| F24 | ~~`toolRegistry` to niezabezpieczona mapa.~~ **Naprawione.** Jedna `sync.RWMutex` plus cztery pomocniki (`lookupTool`, `registerTool`, `unregisterTool`, `toolNames`); `runToolInner` nigdy nie trzyma blokady w czasie `tool.Run`. `luaToolsSchema` przepisane na jedno przejscie pod jedna blokada — wczesniej zbieralo nazwy, a potem indeksowalo mape DRUGI raz, wiec zapis miedzy tymi dwoma dostepami wywracal sie na rzutowaniu typu. `registerLuaToolsFromDir` ma teraz sprawdzenie kolizji i zapis w JEDNEJ sekcji krytycznej. Przekierowane tez ~15 miejsc w testach. **Druga wada, znaleziona przy okazji i nie objeta zgloszeniem:** `SetJobWaiter` mutowalo `WaitTool.Waiter` W MIEJSCU, a `Run`/`waitForJob` czytaly to pole na goroutine'ach zadan — wyscig na polu struktury, ktorego zabezpieczenie mapy nie dotyka. Osobny `waiterMu` plus `waiter()`/`setWaiter()`, `waitForJob` bierze wartosc parametrem. Efekt ubocznie lepszy niz na `main`: sprawdzenie nil i uzycie to teraz ta sama wartosc, wczesniej byly dwa osobne odczyty. **Zweryfikowane niezaleznie:** zdjalem zabezpieczenie z `luaToolsSchema` i test dal 3 raporty wyscigu; koder odtworzyl wariant `fatal error: concurrent map iteration and map write` 5/5; recenzent odtworzyl wyscig B 3/3. Ta sama wada w dwoch przebraniach — brak raportu detektora nie znaczy brak bledu. Poprawiony nieprawdziwy komentarz w tescie (twierdzil, ze `help` nie dotyka rejestru; dotyka DWUKROTNIE, przez `helpIndex` -> `GetAllToolsSchema` -> `luaToolsSchema`). `subagentToolInstance` nie wymaga blokady — recenzent potwierdzil mocniej, niz twierdzil koder: ta zmienna nie ma ANI JEDNEGO czytajacego, jest wylacznie do zerowania w testach. | zrobione |
| F25 | Kolejne globale tej samej klasy co F11, ktorych nie objela ani tura 1 (B2), ani F11 — wyliczone przez recenzenta: `globalMCPRunner` (`tools/mcp.go:318`, pisany przez `InitMCP` i `SetMCPToolRunnerForTests`, czytany przez `GetMCPToolRunner` w czasie wykonywania narzedzia, czyli NA goroutine zadania — plus bezposrednie zapisy z testu w `tools/mcp_test.go:128-130`), `toolsSchema` (`tools/tool.go:617`, pisany w `init()`), `cronRunnerExeOverride` (`tools/cron.go:354`, tylko dla testow), `sidebarSaveVisible` (`display/tui_sidebar_persist.go:10`), `defaultHTTPClient` (`internal/connect/connect.go:32`). | `globalMCPRunner` jest jedynym z tej listy, ktory ma dokladnie ten sam profil ryzyka co siodemka z tury 1 (odczyt na goroutine zadania) — reszta to porzadki. Warte jednej tury razem, bo wzorzec jest mechaniczny i jest juz sprawdzona metoda weryfikacji: cofnij jeden global i potwierdz `WARNING: DATA RACE`. Dopisane po recenzji F24: `SubagentBackgroundAfterSec` (`tools/subagent.go:47`) — czytane na goroutine zadania (`subagent.go:1096`, timer w `runWithHandoff`), zapisywane bez zabezpieczenia przez testy (`subagent_wake_test.go:143-144`, `subagent_handoff_test.go:35,40`); produkcja nie zapisuje, wiec ekspozycja tylko testowa, ten sam ksztalt co `cronRunnerExeOverride`. Recenzent przeszedl wszystkie 60 globali w nietestowych plikach `tools/` i poza ta lista nie ma juz nic niezabezpieczonego. | recenzja F11 punkt 7 + recenzja F24 punkt 8 |
| F26 | `tools/job_globals_race_test.go:180-183` i `:531` sprzataja przez `SetXxx(nil)` zamiast zapamietac i przywrocic. | Nieszkodliwe dzis (nil to stan zerowy tego pakietu, a te haki nie sa podpinane poza pojedynczymi testami), ale po naprawie `withKillWiring` i F12 to jedyne miejsce, ktore zostalo przy starym wzorcu — czyli wzorzec do skopiowania przez nastepna osobe jest teraz niespojny. | recenzja F11, punkt 3 |

## Rejected — do not propose again

- **Permission prompts / approval system.** Does not fit the project: tyci runs
  its tools unprompted by design.
- **Plan mode.** Same reason. The model-facing plan guard and `tools.AllowOnly`
  stay — those are not user approval prompts.

## Undecided (from the competitor comparison)

Custom slash commands from files, git-backed checkpoints with `/undo`, repo map
in the system prompt, diff rendering in the TUI, statusline hook.
