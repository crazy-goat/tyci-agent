# tyci — backlog

Working agreement: one item at a time. A Sonnet subagent implements, an Opus
subagent reviews, the Sonnet subagent fixes what the review found, and that
loop repeats until the review comes back clean. Then the next item.

Status: `todo` · `wip` · `review` · `done`

Work happens in a git worktree, never in the main checkout. A coder subagent
gets `isolation: "worktree"`, the review runs against that worktree, and only a
feature that came back clean is merged into `main`. The reason is not tidiness:
the main checkout is where the person reads code and thinks about what to build
next, and a tree carrying three half-finished features is unreadable for that.
`main` therefore stays committed and clean between items.

The loop feeds itself. Anyone working an item — coder or reviewer — who trips
over a real problem outside their scope does NOT fix it and does NOT stay quiet
about it: it goes in the Inbox below, one line, with a file:line and why it
matters. Fixing it inline would smuggle unreviewed work into an unrelated diff;
dropping it means finding it again in three weeks. Between rounds the inbox is
triaged — promoted into the tables with a size, merged into an existing item, or
struck out with a reason. The queue is expected to grow faster than it drains at
first; that is the point, not a failure.

## Bugs and dead code (cheap, unblock what already exists)

| # | Item | Size | Status |
|---|------|------|--------|
| 12 | `tyci session show` parses the wrong event type and field names (`sessioncmd.go:165-183`): looks for `type:"header"` where the file writes `"session"`, and a `blocks` field where the format has `content` with `toolCall`/`toolResult`. Result: empty header, 0 tool calls. Also fixed on the way: an infinite loop on a corrupt or truncated line (`json.Decoder` never resynchronises), a silently-dropped remainder when a line exceeds the scan buffer, and a test fixture that invented a block type no writer produces. | XS | done |
| 13 | Two loose ends: `stream.Retry` is emitted but never consumed in `runOnce`'s switch; and the subagent tool whitelist check (`main.go:355-366`) omits `alwaysAllowedTools`, so `help`/`lua` appear in a whitelisted child's schema but a direct `help` call is refused. Resolved: `stream.Retry` handling removed rather than half-implemented (see F1), and the gate/schema disagreement fixed from one shared list — including the unrestricted path, where `agents` was reachable through `lua` despite never being offered. | XS | done |
| 18 | **models.dev prices survive a refresh, and a refresh stops destroying what it did not fetch.** Mostly landed: `Cost`/`Limit` are carried through the cache, `RefreshModels` merges instead of overwriting (a hand-maintained gateway provider survives), the write is atomic (temp + rename, mirroring `internal/cron/store.go`), and an import of nothing no longer rewrites the file. **Left over from review:** (a) whole-provider replacement is unconditional, so a degraded fetch serving a provider with zero models silently empties a good priced entry — skip the swap when the incoming provider has no models and the cached one does; (b) `CatalogNeedsPrices` is global where the problem is per-provider, so one priced provider (the user's `nexos`) suppresses the warning for the other 127 unpriced ones forever, and it duplicates `pricing.CatalogPriced` in a second package — better to warn about the model actually in use, which is the only moment the message is true and actionable; (c) untested paths: `--provider x` against an existing catalog, and the unparsable-existing-file refusal that is this item's actual data-loss guard. | S | review |
| 20 | **Every quick blocking subagent runs TWICE.** Present on `main` today, not introduced by our work. `runWithHandoff` (`tools/subagent.go:696`) returns `(ToolResult{}, false)` at line 744 when every child finished inside the 60s window — the common case — which discards the results it already has; the caller at `tools/subagent.go:353` then falls through and re-runs every task from scratch (`:364`). So one `subagent(task=…)` call pays the model twice, performs its side effects twice (file writes included), streams into the parent's tool block twice, and writes two `resumable` transcripts. Verified empirically by the reviewer: runner invoked 2×, and after item 17 it also registers 2 jobs. Fix belongs in `Run`/`runWithHandoff` — return the collected `st.result()`s when `handed == false` instead of falling through — not in the fallback function. Do this BEFORE item 17 is merged: it doubles item 17's blast radius, and items 15/16 build on the same call path. | S | todo |
| 8 | MCP is not wired on live paths: `tools.InitMCP` (`tools/mcp.go:257`) has no caller outside tests, and the agent config uses `GetToolsSchemaJSON()` rather than `GetAllToolsSchemaJSON` (`commands.go:156`). MCP servers are never connected nor advertised to the model. | S | todo |
| 9 | User-defined Lua tools in `~/.tyci/tools/` and `./.tyci/tools/` are loadable and callable through `RunTool`, but their schemas never reach the model — `GetToolsSchema` (`tools/tool.go:117`) is a static list. Also: the parsed `parameters`/`runFunc` are stored and unused, and `Run` re-DoFiles the script on every call. | S | todo |
| 10 | No history compaction and no context-budget accounting. The session format already has a `compaction` event and `session.WriteCompaction`, and replay applies it — nothing ever writes one. Needs `/compact` (with optional focus instructions) and an auto-compact threshold. | M | todo |
| 11 | Cron only fires while an interactive session is open, and the `tyci cron` command that `internal/cron/run.go:77` refers to does not exist. | M | todo |

## Features

| # | Item | Size | Status |
|---|------|------|--------|
| 6 | **Per-project sessions.** Sessions are not global — they are keyed on the exact cwd (`session.DefaultPath`), which is the actual defect: `repo/` vs `repo/sub/` vs each git worktree land in separate pools, so `/resume` loses sessions. Derive the key from the git toplevel (resolving a linked worktree to its main repo via `--git-common-dir` so worktrees share one pool), fall back to abs cwd outside git. Record `ProjectRoot` in the session header, migrate existing dirs by their recorded `Header.CWD`, default listings to the current project with an `--all` escape hatch. | S | todo |
| 4 | **Message a running subagent.** The mechanism exists for the main agent (`agent.Config.NextMessages`, drained after every `runOnce`) and is deliberately nil for children (`btw.go:173`). Add a per-job mailbox to `jobs.Registry` (`Post`/`DrainMessages`), wire it through the `tools` adapter pattern using `JobIDCtxKey`, expose `/msg <job> text` plus a `message` tool so the parent model can steer its own children. Delivery is between model turns, not mid-tool. | S | todo |
| 5 | **Session forking.** Start a child from an arbitrary point in the current history. `btw.go:154 forkMessagesForBtw` already deep-copies history and `session.sanitizeMessageSequence` already repairs a cut that lands inside a tool-call/result pair. Address the fork point by transcript index (live) or session event id (persisted); ship fork-as-background-child first, fork-as-new-session second; add `inherit_history` to the subagent tool. | S–M | todo |
| 3 | **`/btw` → fork as session.** `/btw` stays read-only (it asks "is this worth doing?" and must not modify anything). When the answer is yes, the side-conversation is promoted to a real subagent, and the main thread is told a new subthread exists and that it should wait for it. Depends on #4 and #5. | M | todo |
| 2 | **`@agent:` in completions.** One list offering both files and agents: picking a file inserts `@file:<path>`, picking an agent inserts `@agent:<name>` and instructs the model to continue that work in a subagent. The engine already supports agent selection (`subagent`'s `agent` field, `.tyci/agents/*.md`) — this is the missing UI. | S | todo |
| 1 | **Right-side sidebar**, toggleable, with tabs: Tokens, Sessions, Bash, Lua. Clicking the context figure in the status bar opens the Tokens tab. The detail that used to sit in the status bar is already rendered by `display.buildUsageDetail` and is waiting for a place to live. Bash and Lua tabs need per-run ledgers (command/script, start time, state) — the point is watching long-running work. Add a **Subagents** tab too: the history of children, what each produced, and a way to re-enter the conversation at that point. Most of that already exists but only the model can reach it — `tools/resume.go` continues a finished job's exact conversation as a new job, backed by the `resumable` transcript map in `main.go:59-79`. Two things to sort out: that map is never pruned (a long session keeps every child's full transcript in memory, which is the same class of leak as the conversation history), and it is keyed by job id, so children taking item 17's fallback path have no entry. Invasive in the render path (`display/tui_view.go` builds full-width rows; caches are width-keyed; mouse hit-testing must clamp to the main column), so it goes last. | M–L | todo |
| 17 | **Every child gets a job id, in every mode.** Prerequisite for items 15, 16, 4 and the sidebar's Subagents tab, and much smaller than it first looked: blocking children in an interactive session are *already* registered before their first iteration (`tools/subagent.go:596-599`, and the comment at `:64-69` says this is deliberate — "so ask/report_progress work there too, for free"). The gap is only the fallback at `tools/subagent.go:352-358`, taken when `jobStarter == nil` or `!backgroundAllowed(ctx)` — that is `--print`/`tyci run` (`main.go:30`) and every child-of-child (`backgroundAllowed` returns false whenever `SubagentSinkCtxKey` is set, `tools/bgbash.go:92-97`). Those drop to plain `runTasks` with no id, so `report_progress`, `ask` and `resume` all refuse. Fix: register them through `jobStarter.Start` as well, skipping only the 60s handoff, not the registration — `jobStarter` is wired in every mode (`main.go:424`). Retention is already bounded (50 terminal jobs, `jobs/registry.go:239-249`) and inline finishes already suppress the finish notice (`tools/subagent.go:604-612`), so nothing downstream needs changing. Second half of the job: the error text in `tools/progress.go` and the copy in `providers/provider.go:111` and `tools/help_articles.go` still say "only works inside an async background job", which is already false for the sync path — the model reads that and believes it. **Review round 2 found three things before this can merge:** (a) HIGH — giving these children a job id makes `ask` pass its gate on exactly the path where no answer can ever arrive, so a child that asks a question now blocks for the full 600s timeout and then dies, where before it failed instantly and carried on; `ask` needs a separate signal from "has a job id" (e.g. a ctx flag set only by modes that actually drain `JobNotices`). (b) MEDIUM — the "job is terminal before Run returns" claim is a race: `close(done)` happens inside `fn`, while the registry sets status and closes `job.done` only after `fn` returns, so two of the new tests fail under `-count=1000`; wait on the registry's own completion signal instead of inventing a second one. (c) MEDIUM — the child-of-child motivation is **fiction**: `subagentDeniedTools` blocks `subagent` in both the child's schema and its runtime gate, so grandchildren do not exist in this codebase. The only reachable path is `--print`/`tyci run` plus item 20's fallthrough bug, and three of the five new tests pass with the fix reverted. Rewrite the justification and the tests around the path that is real. | S | review |
| 16 | **A finished child says how to come back to it.** When a subagent ends, append a line to the result telling the parent it can continue that exact conversation later — the `resume` tool already does this, backed by the transcript map in `main.go:59-79`, and today the model has to know that on its own. The precedent is the worktree note (`tools/subagent.go:961-983`) which already tells the parent which branch to look at. The value is the follow-up question: the user asks "and what about X?" an hour later, and the parent can hand the work back to the child that already has the context instead of starting over. **The note must not lie:** it needs a real job id. Blocking children in an interactive session already have one (`tools/subagent.go:596-599` registers before the first iteration), so this covers nearly everything; the exception is item 17's fallback path, where there is no id and so no note. Keep the wording short; it is appended to every child's output and the parent pays for it in tokens. | S | todo |
| 15 | **A subagent must report in every N iterations.** The plumbing exists and is voluntary: `report_progress` (`tools/progress.go`) posts a note onto the job (`jobs/registry.go:222`), surfaced by `wait` and the jobs panel — and a child that never calls it is silent for ten minutes. Make it periodic and enforced: the agent loop knows its iteration count, and `agent/agent.go:290-322` already injects harness reminders as user messages (todos, pending jobs), so the same machinery can inject "you are N iterations in, post one line on where you are" every 20 — with the same max-per-turn discipline so it cannot crowd out the real conversation. Fire-and-forget by design: the parent never has to answer, it just stops being blind. **Decided (see item 17 for the shared prerequisite):** the reminder is **time-based, not step-based** — prompt for a note at the next iteration boundary when more than 60s have passed since the child's last one, reusing `SubagentBackgroundAfterSec` as the constant. Iterations vary from one second to ten minutes, and `MaxIterations` defaults to unlimited (`agent/agent.go:31`), so both a flat count and a fraction of the cap are meaningless. Temporal settled this argument for activity heartbeats years ago for exactly this reason. Trap: track "time since last note" on the `Job` (extend `jobs.Job`), not in the agent loop — a child that never reports must not be nagged every single iteration. Pairs with item 4, which is the opposite direction. | S–M | todo |
| 19 | **Thinking collapsed by default, like a tool.** A thinking block should render as one line — `thinking(Find all usages of validateOptions…) 42ms - click to display` — and open on click, exactly as tool blocks already do. The machinery is there: blocks carry a `collapsed` flag (`display/tui.go:128`), `display/tui_render_tool.go:36` already renders the "- click to display" affordance, and the click-to-expand modal already exists (`tui_modal_content.go`, reached from `tui_mouse.go`). Two things do not: a thinking block has no duration (the `duration` field is only set for `tool-end`), so the TUI has to time it itself from the first thinking delta to the arrival of the next text or tool block; and the one-line summary has to come from the content's opening words, collapsing newlines, which `tui_resume_picker.go:338` already has a helper shape for. Watch the streaming case: thinking arrives as deltas into a growing block, so it must render collapsed while streaming without the line flickering on every delta. The spacing half of this is already done (see Done). | S–M | todo |
| 14 | **Model variant / thinking mode.** There is no way to ask for extended thinking or set its budget. `connector.Request` carries only `Temperature` and `MaxTokens` (`connector/message.go:41`); no connector sends a thinking or reasoning parameter — Anthropic wants `thinking.budget_tokens`, OpenAI-shaped APIs want `reasoning_effort`, Gemini wants `thinkingConfig`. So the field has to reach `Request`, each connector maps it its own way, and the catalog should say which models support it. Then the UI: a way to pick it per session (the model picker or a `/think` command), per named agent (frontmatter, next to `temperature`), and per subagent call. Note the related known bug — every converter currently *discards* thinking blocks on the way back in (docs/architecture-refactor.md:1022), so turning thinking on without fixing that buys tokens and shows nothing. | M | todo |
| 7 | **`agent.lua`** — a Lua-scripted conversation process for repeatable tasks. `internal/workflow/engine.go` is a complete orchestration engine with `tyci.run_tool/new_session/resume_session` and session `prompt/await/save`, and has no caller outside its own test. Wire a CLI entry point and `.tyci/agents/*.lua` discovery, then close the real gaps: sessions get no tools and no schema, `MaxIterations` is hardcoded to 10, and there is no `subagent`/`wait` sugar for fan-out. | M | todo |

## Done

| # | Item |
|---|------|
| — | Git branch in the top bar, resolved correctly inside linked worktrees (`internal/gitinfo`). |
| — | No blank line inside a run of tools and thinking: the spacer rule is now one shared helper (`display/tui_render_buffer.go` `spacerAfter`) instead of the same condition duplicated in both flat-line builders, where a disagreement would have shifted every later line index and sent mouse selection to the wrong row. |
| — | Status bar right side reduced to context used + session cost including subagents. Backed by `internal/pricing` (costs and context limits from the models.dev catalog) and `internal/ledger` (process-wide spend, main separated from delegated). |

## Inbox — found while working, not yet triaged

Format: `F<n> | what, file:line | why it matters | found by`.

| F | What | Why it matters | Found by |
|---|------|----------------|----------|
| F2 | `tools/ask.go:43` carries the same stale text `report_progress` had: "ask only works inside an async background job (started via subagent(...,async:true) or a /btw side-conversation)". After item 17 that is false — every subagent call has a job id. | The model reads the error and concludes the tool is unavailable to it, so a blocking child that needs to ask the user a question gives up instead of asking. One-line fix. | coder, item 17 |
| F1 | ~~Nothing constructs a `stream.Retry`~~ — **resolved: the type is deleted.** Retrying belongs to the agent loop, which already reports every attempt through the Sink (`agent/agent.go:188`); connectors return `api.RetryableError` upward rather than retrying inside a stream. A comment in `stream/stream.go` now records why there is no such event, so it does not come back. | done |

## Rejected — do not propose again

- **Permission prompts / approval system.** Does not fit the project: tyci runs
  its tools unprompted by design.
- **Plan mode.** Same reason. The model-facing plan guard and `tools.AllowOnly`
  stay — those are not user approval prompts.

## Undecided (from the competitor comparison)

Custom slash commands from files, git-backed checkpoints with `/undo`, repo map
in the system prompt, diff rendering in the TUI, statusline hook.
